# Blawgy Clone Progress

Date: 2026-08-14

## Current Status

Sir Bloggsalot has a working local account dashboard with Google auth, protected `/account` routing, Blawgy-style navigation, persisted local account data, and local implementations for Content Plan, Write, Topics, Settings, Billing, Search Console, Rankings, AI Mentions, Reports, SEO Analysis, Help, workspace switching, invites, and support tickets.

The remaining 1:1 clone work is fidelity work against authenticated Blawgy evidence. The current screenshot set is useful, but it does not prove every dashboard pocket. The current capture request remains `docs/blawgy-missing-screenshot-pockets-2026-08-14.md`.

## Evidence Already Covered

- Public Blawgy homepage and blog offer structure.
- Public homepage limited-time promo bar slice: the first fold now includes the screenshot-backed black announcement bar with `Limited Time`, the August 31 annual-pricing deadline, `Start free trial` CTA, and dismiss X, with SBA branding preserved and localStorage-backed dismissal behavior.
- Base authenticated Blawgy shell/sidebar from the existing screenshots.
- Site Settings form layout and copy.
- Products empty state layout.
- Content Plan card/grid layout with onboarding overlay.
- Dashboard screenshot vocabulary restoration slice: local `/account` now uses the fuller Blawgy sidebar evidence from `image-11.png`, `image-12.png`, `image-16.png`, and `image-17.png`: a `CREATE` group with `Content Plan`, `Write`, and `Topics`; a `TRACK` group with `Rankings` and `AI Mentions` Pro+ badges plus `Search Console`; then visible `Settings`, `Getting started`, `Billing`, `Help`, and `Log out`. Reports and SEO Analysis remain route-accessible hidden local panels until Blawgy sidebar evidence shows where they belong.
- Sidebar brand mark parity slice: the account sidebar keeps the SBA `S` brand mark but now uses the Blawgy screenshot's mint speech-bubble silhouette with a small tail instead of the old plain square badge.
- Sidebar hidden local panels cascade fix: the route-compatible Reports and SEO Analysis buttons now have a scoped hidden-nav CSS override so they stay out of the visible Blawgy sidebar while their panels, aliases, APIs, and tests remain available.
- Sidebar workspace selector parity slice: the account switcher now follows the Blawgy sidebar capture more closely with a larger rounded selector card, circular workspace/avatar image treatment with an `S` fallback, truncated workspace label, and stacked up/down chevrons.
- Sidebar workspace selector cascade fix: removed the stale legacy 20px workspace-icon override so the Blawgy-style circular selector image treatment is not undone later in the stylesheet.
- Sidebar Write visible-nav parity slice: the internal `write` sidebar button is visible again as `Write`, matching the authenticated Blawgy sidebar crop, while the hidden Content Plan `Article Builder` compatibility action still routes through the existing fresh-draft reset flow.
- Sidebar Create shell parity slice: the old local Create dropdown remains hidden compatibility markup, but the visible sidebar now includes Blawgy's static `CREATE` section label and the `Content Plan`, `Write`, and `Topics` route controls.
- Sidebar Create shell hidden cascade fix: the old local Create dropdown now has a scoped hidden-shell CSS override so it stays out of the visible Blawgy sidebar while its hidden compatibility actions remain available to the existing action harness.
- Getting started tooltip parity slice: the visible Getting started sidebar item now exposes the screenshot-backed guided-walkthrough tooltip copy while preserving the existing setup checklist and tour routing.
- Settings sidebar icon parity slice: the visible Settings sidebar item now uses the screenshot-backed sliders-style icon instead of the older local gear icon while preserving the existing settings view and routes.
- Sidebar line-icon parity slice: the visible account sidebar now uses a shared SVG line-icon sprite for Content Plan, Write, Topics, Rankings, Search Console, AI Mentions, Settings, Getting started, Billing, Help, and Log out instead of local text glyphs, matching the existing Blawgy sidebar crops while preserving all route/action bindings.
- Content Plan first-fold parity slice: the Articles panel now uses the Blawgy screenshot heading `Content Plan`, inline `scheduled · writing · published` stats, visible Strategy, grid/list, refresh, overflow, rolling date-window, and `+ Add topics` controls. Bulk Schedule and Article Builder remain wired as hidden compatibility actions until exact Blawgy overflow/menu placement is captured.
- Content Plan screenshot-source correction slice: after re-inspecting `image-25.png`, the first-fold toolbar was restored to the screenshot-backed Strategy/grid-list/refresh/overflow/date-window/Add topics layout instead of the contradictory board-note-only Bulk Schedule/Article Builder layout.
- Content Plan summary-copy parity slice: the dynamic Content Plan summary now matches the Blawgy screenshot/static fallback copy: `Articles write and publish themselves on their dates. New topics are added for you every week. You can jump ahead anytime.`
- Content Plan strategy-card visibility parity slice: the `Why this plan` card now appears by default in static and dynamic Content Plan first-fold states, matching the Blawgy screenshot, while the existing dismiss control hides it for the current dashboard session.
- Content Plan strategy-copy parity slice: the dynamic `Why this plan` fallback now uses the Blawgy screenshot phrasing for keyword research topics, posting spacing, and rough monthly visit range while preserving any custom saved Strategy text.
- Content Plan Strategy icon parity slice: the first-fold Strategy toolbar button now includes a screenshot-backed sliders icon before the label while preserving the existing Strategy action wiring.
- Content Plan toolbar icon parity slice: the visible overflow, grid, list, and refresh controls now use compact icon-only toolbar treatments with the existing accessible labels and action hooks, matching the Blawgy Content Plan command cluster more closely than the old text glyphs.
- Content Plan grid-default parity slice: the Blawgy screenshot calendar/card grid now renders by default, with the grid control active before account data loads and new accounts defaulting to grid mode; list/table remains available through the visible list control, including Previous/Today/Next window controls and the `+ Add topics` action, with render/API coverage in `scripts/check-content-plan-render.js` and `scripts/check-account-api.js`.
- Content Plan card-source parity slice: calendar cards now include the screenshot-backed source footer, `Added by Sir Bloggsalot`, preserving allowed SBA branding in place of Blawgy branding in both dynamic render output and static fallback markup.
- Content Plan card-difficulty wording parity slice: calendar cards now display the Blawgy screenshot wording `Easy to rank` for locally stored `Easy` difficulty values while leaving the underlying saved data and Keyword Finder labels unchanged.
- Content Plan card-visits emphasis parity slice: calendar cards now render projected visits as a dedicated green emphasis line, matching the Blawgy screenshot treatment while preserving the existing estimated-visit calculation.
- Content Plan card-footer parity slice: calendar cards now use the Blawgy screenshot footer structure with a standalone date pill and right-aligned `Added by Sir Bloggsalot` source label, removing local status/draft suffixes from the visible card footer while keeping status controls in the table/detail flows.
- Content Plan row-action parity slice: table rows now use the compact Blawgy screenshot primary action pattern. Scheduled/published rows expose `View article`, `Edit article`, and red `Delete article` icon actions; generated/processing rows expose `Generate` or `Regenerate` plus red `Delete article`. The extra local workflow actions remain covered inside the row `More` menu, and `View article` opens a protected read-only article preview from the existing detail API.
- Article detail modal parity slice: `View article` now opens a structured in-app `Article detail` modal instead of plaintext preview copy. The modal shows status, excerpt, readiness/missing-field guidance, metadata, SEO/canonical/public-path fields, draft body, and the existing Edit, Open in Article Builder, Schedule, Generate/Regenerate, Publish/Unpublish, and Delete lifecycle actions through the current protected blog action wiring. It mirrors protected publish blockers for missing title, slug, body, or future schedule by disabling Publish and showing the backend-aligned reason; row-level Publish now uses the same blocker before opening confirmation. Direct Open in Article Builder and Generate/Regenerate from the detail modal now refresh/switch state and close the modal so stale article details are not left onscreen.
- Content Plan calendar-card detail parity slice: calendar cards now open the same protected `Article detail` modal as table-row View instead of the older direct Edit article form, so list/table and grid/calendar states share one lifecycle surface for edit, schedule, generate/regenerate, publish/unpublish, delete, and Article Builder handoff.
- Article Builder label parity slice: the internal `write` panel now renders the Blawgy screenshot heading `Article Builder`, keeps the visible `Generate article` primary action, and preserves the covered local draft/save/preview/start action wiring.
- Content Plan Article Builder compatibility slice: the Content Plan keeps the existing `data-blog-action="article-builder"` route as hidden compatibility markup while the visible sidebar `Write` item and walkthrough now match the authenticated Blawgy captures.
- Article Builder generated action rail parity slice: generated or opened Article Builder drafts now reveal an in-context action rail with Save draft, Schedule, Publish, and Regenerate controls. Schedule, Publish, and Regenerate stay disabled until the builder is tied to a saved article record, then reuse the protected local blog post schedule/publish/generate endpoints and return the operator to Article Builder.
- Article Builder generated editor shell parity slice: the internal `write` panel now groups draft body editing into a Blawgy-style editor shell with readiness, schedule, and public URL summary blocks plus SEO and featured-image side panels, while preserving every existing `data-write-field` binding and generated action behavior.
- Keyword Finder label parity slice: the internal `topics` panel now renders the Blawgy screenshot heading `Keyword Finder`, `Find high-value keywords...` subtitle, and `keywords found` summary while preserving keyword search/save/filter internals and `/keyword-finder` compatibility.
- Keyword Finder filter-card parity slice: the visible filter card now matches the Blawgy screenshot structure with four grouped metric headings, `DIFFICULTY (KD)`, `CPC ($)`, `VOLUME`, and `COMPETITION`, each exposing Min/Max inputs; the visible search field was removed from the card while local keyword search compatibility remains behind the existing hidden action/test harness.
- Google Search Console label parity slice: the internal `search` panel now renders the Blawgy screenshot heading `Google Search Console` and disconnected status copy while preserving Google OAuth/Search Console action wiring and `/google-search-console` compatibility.
- Google Search Console header-action parity slice: the connected header now matches the visible Blawgy screenshot pattern with only the date-range selector and an icon-only disconnect control; Sync and Export remain wired as hidden local compatibility actions.
- Billing sidebar label parity slice: the lower account nav now shows `Billing`, matching the authenticated Blawgy sidebar crop, while preserving the existing internal billing view, checkout handoff, and `/subscribe`/`/upgrade` compatibility aliases.
- Getting started sidebar restoration slice: the visible sidebar now includes `Getting started`, matching the Blawgy sidebar crop, while the internal `getting-started` panel and `/tours`/`/getting-started`/`/onboarding` compatibility routes remain available for setup checklist and walkthrough access.
- Tour overlay parity slice: the walkthrough overlay now uses the Blawgy-style 11-step progress model with dot indicators, `1 of 11` copy, fixed modal/backdrop presentation, and the screenshot-backed sequence from the available captures: Welcome, Article Command Center, Filter by Status, Bulk Schedule Articles, Write, Topics Worth Writing, Settings, Customize Your Content, CMS Connect, Image Settings, and `You're All Set!`. SBA branding is preserved in visible copy, and the existing Back/Next/Finish routing across dashboard panels remains covered.
- Tour overlay close-control parity slice: the walkthrough overlay now includes the Blawgy screenshot top-right `×` close control, dismisses from any step, and keeps Back/Next/Finish behavior intact.
- Tour overlay no-eyebrow parity slice: the walkthrough overlay keeps its internal step eyebrow data hook, but hides the visible `Dashboard tour` eyebrow so the modal starts with the screenshot-backed welcome/title line.
- Tour overlay centered-modal parity slice: the walkthrough modal is now centered over the dashboard content area from the Blawgy capture instead of being anchored from the lower-right corner, while preserving the mobile bottom placement.
- Tour overlay modal-width parity slice: the walkthrough modal width cap now matches the available Blawgy welcome capture more closely at a wider desktop modal while keeping the existing mobile viewport guard.
- Tour overlay corner-radius parity slice: the walkthrough modal now overrides the generic dashboard card radius with the larger rounded corner treatment visible in the Blawgy welcome capture.
- Tour overlay first-step-actions parity slice: the walkthrough overlay first step now hides Back and shows only the primary Next action, matching the Blawgy welcome capture, while later steps restore Back.
- Tour overlay pointer parity slice: sidebar-targeted walkthrough steps now show the Blawgy-style left pointer triangle from the settings/topics captures, while the welcome step remains pointerless and the mobile bottom-sheet layout suppresses the pointer.
- Tour overlay target-highlight parity slice: sidebar-targeted walkthrough steps now raise and outline the matching sidebar nav item above the dimmed backdrop, matching the Blawgy settings/topics highlight captures while clearing the target on welcome, close, and finish.
- Tour overlay sidebar-highlight fade parity slice: sidebar-targeted walkthrough steps now add the Blawgy-style right-edge white fade inside the raised nav target, matching the Settings/Write/Topics highlight captures while leaving normal sidebar items untouched.
- Tour overlay settings-tab target parity slice: Settings substeps now raise and outline the active Settings tab, such as Site Settings, CMS Connect, and Image Settings, instead of incorrectly keeping the left Settings sidebar item highlighted. This matches the Blawgy tab-strip tour captures while preserving the sidebar highlight for the general Settings step.
- Tour overlay tab-strip highlight parity slice: the Site Settings `Customize Your Content` tour step now raises and outlines the full Settings tab strip, matching the Blawgy capture where the whole tab row is highlighted, while CMS Connect and Image Settings keep their single-tab highlights.
- Tour overlay tab-pointer parity slice: Settings substeps with a highlighted tab now use a top pointer from the modal toward the tab strip, matching the Blawgy Settings-tour captures, while sidebar-targeted steps keep the left pointer and mobile suppresses both pointer treatments.
- Handoff comparison matrix tour-evidence parity slice: the main Blawgy comparison checklist now records the full Settings tab-strip highlight and top tab-pointer for settings-tab tour steps instead of underreporting the implemented tour overlay state.
- Tour overlay target-scope parity slice: Content Plan command/filter/bulk walkthrough steps now stay pointerless and do not raise the sidebar item, matching the available content-area modal captures; only screenshot-backed sidebar steps keep the pointer/highlight treatment.
- Tour overlay footer parity slice: the walkthrough modal footer now matches the Blawgy welcome capture more closely with a single divider-backed footer row containing the dots, `1 of 11` progress copy, and Back/Next/Finish actions together instead of splitting actions into a separate row.
- Tour overlay next-button parity slice: the walkthrough primary Next action now includes the Blawgy-style right chevron in the button while preserving existing Back/Next/Finish behavior and accessible close/step controls.
- Tour overlay primary-button sizing parity slice: the walkthrough primary action now has a tour-scoped wider/taller button treatment with a slightly larger radius, matching the Blawgy welcome capture without changing global account buttons.
- Account topbar hidden-shell parity slice: the authenticated account metadata shell stays in the DOM for state plumbing, but the visible topbar is hidden so dashboard pages begin directly with their Blawgy-style page title and subtitle.
- Account topbar hidden cascade fix: the hidden account metadata shell now has a scoped CSS override so the later `.account-topbar { display: flex; }` rule cannot make it visible above Settings tabs, matching the Blawgy authenticated screenshots.
- Products empty-state parity slice: the Products tab keeps Blawgy's disconnected empty-page structure with a neutral source card showing only `+ Connect`, a visible `+ Add Product` action, All/Synced/Manual/Hidden filters, product search, and the `No products yet` / `Add your first product` empty state. Sync/Disconnect and disconnected source details stay hidden until a source is connected, stale hidden product filters/search reset to All/blank, connected/sync/disconnect local behavior remains covered, and account payloads scrub blocked legacy inventory/source labels before rendering.
- Products empty-state cube-icon parity slice: the dynamic Products no-product and no-match empty states now use the screenshot-backed gray cube line icon from the shared SVG sprite instead of the old text glyph, preserving provider-neutral source-card copy and the blocked legacy provider/industry cleanup.
- Products source-card icon parity slice: the neutral Products source card now uses a mint storefront line icon from the shared SVG sprite instead of a placeholder square glyph, matching the Blawgy Products first-fold card treatment while retaining the provider-neutral Inventory Feed label required by the SBA cleanup.
- Products populated-row badge parity slice: populated product rows now expose compact source, featured, and visible/hidden badges so manual and synced catalog states scan like a dashboard list instead of a plain local action row, while preserving the existing detail, edit, hide/show, delete, filter, and search behavior.
- Inventory feed failure-state parity slice: inventory/source detail rows now surface local provider failures as a distinct `Needs attention` badge with the stored failure reason, while connected/disconnected rows keep compact status badges and no live provider success is faked.
- Site Settings visible sequence parity slice: the Site Settings tab now follows the Blawgy screenshot's first-fold sequence: Product Description, Target Audience helper copy, Your keywords helper copy, Keyword mix helper copy, then the additional advanced SBA fields below that visible sequence.
- Site Settings Generate using AI icon parity slice: the Product Description `Generate using AI` action now uses the shared SVG sparkle line icon instead of a local text glyph, matching the visible Blawgy settings capture while preserving the existing generate-description action.
- Site Settings Product Description header-row parity slice: the Product Description label and `Generate using AI` action now share a single first-fold header row, matching the visible Blawgy settings capture while preserving the local progress indicator and textarea binding.
- Site Settings keyword-helper parity slice: the `Your keywords` helper now includes the screenshot-backed guidance about a 0% keyword mix writing only from discovered topics, using audience search terms, and finding good keywords, with SBA branding preserved.
- Site Settings keyword Add row parity slice: the `Your keywords` textarea and Add action now use the Blawgy screenshot's side-by-side input row with a taller dark Add button while preserving the local add-keywords mutation, empty-draft validation, saved keyword chips, and mobile stacking.
- Settings form-control typography parity slice: Settings input, textarea, and select values now use regular-weight form text like the Blawgy Site Settings capture while preserving bold labels, primary buttons, validation, and all existing account field bindings.
- Image Settings first-fold parity slice: the Image Settings tab now follows the Blawgy screenshot's first fold with a collapsed Test Image Generation card, In-Article Images heading/subtitle, In-Article Image Style card, reuse-style checkbox row, In-Article Image Guidelines card, default-guidelines radio row, and visible critical-guidelines text. Existing local image toggles, presets, cadence, guidelines editor, test action, progress, preview, and history remain wired through hidden compatibility markup until generated-output screenshots are captured.
- Image Settings image-style controls parity slice: the visible Image Settings panel now also exposes the `/settings/image-style` screenshot-backed controls for Include Images in Articles, Product Images, Visual Style, and Image guidelines using the existing local `images.*` bindings, while preset/aspect/cadence and prompt-history actions remain hidden compatibility controls until deeper generated-output screenshots are available.
- Image Settings route split parity slice: the screenshot-backed Include Images/Product Images/Visual Style/Image guidelines controls are now visible only when the protected account URL carries `section=image-style`, so `/settings/image-style` starts with that section while `/settings/images` and `/settings/image-settings` keep the separate Blawgy generic Image Settings first fold without duplicating the image-style cards below it.
- Image Settings image-style tools/toggle parity slice: `/settings/image-style` now includes the compact Visual Style reset/suggestion utility buttons and a persisted `Use custom guidelines` toggle that keeps the guidelines editor hidden until custom constraints are enabled, matching the visible Blawgy Image Style card behavior more closely.
- Image Settings image-guidelines helper-copy parity slice: `/settings/image-style` now uses the screenshot-backed Image Guidelines helper copy, `Add specific constraints for what should or shouldn't appear in generated images`, while preserving the existing custom-guidelines toggle and hidden editor binding.
- Image Settings product-image empty-state parity slice: the `/settings/image-style` Product Images toggle now renders off and disabled until at least one product exists, matching the captured empty Product Images state instead of allowing an unusable preference to stay on.
- Image Settings provider-error preview slice: stored image preview/provider failures now render a distinct `Needs attention` badge with the saved failure reason, while local and provider-pending previews keep compact status badges and no real generated image success is faked.
- Handoff comparison matrix Image Settings evidence parity slice: the main Blawgy comparison checklist now records the generic Image Settings first fold, `/settings/image-style` split, custom-guidelines toggle/helper copy, and provider-error preview behavior instead of underreporting the implemented image settings state.
- CMS Connect first-fold parity slice: the CMS Connect tab now follows the Blawgy screenshot's sparse first fold with only Website URL and Website Platform controls visible above the fold. Existing local CMS connect/test/disconnect actions, status, progress, and details remain wired through hidden compatibility markup until provider-specific form and error screenshots are captured.
- CMS publishing-mode dialog parity slice: the local Connect CMS dialog now exposes a `Publishing mode` select with `Draft first` and `Auto-publish after approval`, sends the existing backend `draftFirst` boolean, and reflects auto-publish/draft-first state in the CMS detail row without claiming real provider publishing.
- CMS provider-error state parity slice: CMS detail rows now surface stored local provider/setup failures with a distinct `Needs attention` badge and the failure reason, while connected/local-setup rows keep compact status badges and real provider success remains explicitly pending.
- Handoff comparison matrix CMS Connect evidence parity slice: the main Blawgy comparison checklist now records the sparse Website URL/Website Platform first fold, Publishing mode dialog, provider-pending tests, disabled disconnected controls, and `Needs attention` provider-error behavior instead of underreporting the implemented CMS Connect state.
- Business Locations empty-state parity slice: the Business Locations tab now follows the Blawgy screenshot's empty-state structure with the `Business locations` title, Pages-generator-and-Write explanatory copy, centered bordered empty card, `No locations yet` text, generated-pages-and-articles helper copy, and `+ Add a location` CTA while preserving local add/edit/delete location behavior.
- Business Locations empty-state shield-icon parity slice: the empty card now uses the screenshot-backed mint circular shield/check SVG treatment instead of the old placeholder glyph in both static markup and dynamic no-location renders.
- Pages generator route-compatible parity slice: `/pages`, `/page-generator`, and `/pages-generator` now canonicalize into a protected hidden-nav `Pages` account panel. The panel fulfills the Business Locations tab promise by rendering local page targets from saved locations and site keywords, exposing per-target `Location` detail and `Draft page` actions, and seeding the existing Article Builder with a local landing-page brief while preserving the current screenshot-backed visible sidebar.
- Handoff comparison matrix Business Locations evidence parity slice: the main Blawgy comparison checklist now records the Business Locations empty state, shield/check icon treatment, and `/pages`/`/page-generator`/`/pages-generator` route-compatible Pages workflow instead of underreporting the implemented locations state.
- Call to Action first-fold parity slice: the Call to Action tab now follows the Blawgy screenshot's sparse first fold with the left enable toggle, `Call to Action` heading, right-aligned `Edit CTA` action, and a CTA Best Practices card using the visible four-line guidance. The local CTA fields, preview, reset, validation, and saved settings remain wired through hidden compatibility markup and the existing Edit CTA modal.
- Handoff comparison matrix CTA evidence parity slice: the main Blawgy comparison checklist now records the Call to Action first-fold shell, left enable toggle, right-aligned `Edit CTA`, CTA Best Practices card, hidden compatibility wiring, and local Edit CTA modal instead of underreporting the implemented CTA state.
- Invite Users first-fold parity slice: the Invite Users tab now follows the Blawgy screenshot's first fold with a single Email Address row, disabled-until-email `Generate Invite Link` button, hidden compatibility role selector, exact manual-send helper copy, Current Team Members heading, and expanded invitation-link instructions. Existing local invite generation, copy, revoke, role/member controls, and team activity remain wired below that visible first-fold structure.
- Invite Users member-avatar parity slice: accepted team member rows now include a grey circular initial chip before the member name/email, matching the Blawgy Invite Users capture while preserving existing detail, role-change, and remove actions.
- Invite Users pending-invite avatar parity slice: pending invite rows now include the same Blawgy-style grey circular initial chip and compact row grid before the invite email/status/details while preserving existing detail, copy-link, and revoke actions.
- Invite Users empty-list visibility parity slice: the accepted-member, pending-invite, and Team Activity dynamic blocks now stay hidden when they have no rows, matching the Blawgy first-fold capture where invitation-link guidance follows Current Team Members directly, while preserving member, invite, and audit rows when data exists.
- Invite Users guidance icon parity slice: the invitation-link guidance card now includes the Blawgy-style leading dark circular info icon while preserving the existing manual-send copy and local invite behavior.
- Invite Users divider parity slice: the Invite Users tab now includes the Blawgy-style horizontal divider between the email/helper row and Current Team Members while preserving the hidden role compatibility control and local invite/member behavior.
- Settings full-width panel parity slice: Settings tabs now use the full dashboard content width instead of the old narrow local panel cap, and the Invite Users email/input row expands across that width like the Blawgy Invite Users capture while preserving responsive stacking on mobile.
- Invite Users guidance-card spacing parity slice: the invitation-link guidance card now uses the taller Blawgy-style card treatment with roomier padding and larger wrapped explanatory copy while preserving the existing title, icon, and local invite behavior.
- Invite Users button-width parity slice: the Generate Invite Link action now uses the wider Blawgy-style button treatment from the Invite Users capture while preserving disabled-until-email behavior, full-width input layout, and mobile stacking.
- Handoff comparison matrix Invite Users evidence parity slice: the main Blawgy comparison checklist now records the Invite Users first-fold row, Generate Invite Link action, Current Team Members heading, invitation-link guidance, member/pending invite avatars, hidden empty dynamic blocks, guidance icon/divider/spacing, and full-width Settings layout instead of underreporting the implemented invite/team state.
- Post-checkout success preparation parity slice: `/success` now renders a Blawgy screenshot-backed trial-start and content-plan-preparation handoff with a compact checkout-success header, `Your plan is being prepared`, preparation steps, and an `Open your content plan` CTA into the protected Content Plan. SBA branding is preserved and real Stripe/payment confirmation remains explicit integration work.
- Checkout success exact-copy parity slice: `/success` now follows the Blawgy checkout-success capture more tightly with the compact topbar banner, `You're all set` headline, exact 10-minute content-plan-building copy, yellow still-building warning box, full-width `Open your content plan` CTA, and website-platform footnote. SBA branding and the protected Content Plan destination remain.
- Checkout success CTA icon parity slice: the `/success` completion CTA now uses a shared calendar line icon before `Open your content plan`, matching the Blawgy checkout-success capture while preserving the protected Content Plan destination and SBA branding.
- Checkout success preparation-state parity slice: `/success?state=preparing` now keeps the Blawgy preparation capture available as a route state with `Your plan is being prepared`, the month-of-content preparation copy, Scoring/Scheduling/Writing/Putting-together steps, `This usually takes under a minute`, and the `4s` timer copy. The default `/success` state remains the captured `You're all set` completion screen.
- Billing checkout trial-copy parity slice: local Billing checkout dialogs now mirror the captured Stripe trial framing with `Try Sir Bloggsalot <plan>`, `3 days free`, monthly renewal copy, and `Unlimited access: Publish AI blog posts to your site daily`, while still stating that real Stripe Checkout and Link payment-method management need provider integration.
- Billing checkout trial-start parity slice: local checkout state now records a 3-day `trialEndsAt` date and the checkout dialog renders the captured renewal framing, such as `Then $79.00 per month starting August 17, 2026`, with the authenticated billing handoff cleaning the URL back to `/account?view=billing` after saving the local checkout state.
- Handoff comparison matrix Billing evidence parity slice: the main Blawgy comparison checklist now records the implemented Billing trial dialog, 3-day `trialEndsAt`, monthly renewal copy, `/success` completion/preparation states, `Open your content plan` CTA, invoice deep links, and cancel/reactivate progress feedback instead of underreporting the implemented billing state.
- Help bubble icon parity slice: the authenticated dashboard's lower-right support bubble now uses a shared chat-bubble line icon inside the black circular button, matching the visible Blawgy tour/dashboard capture while preserving the local support-ticket action and explicit no-live-chat provider state.
- Image Settings route-alias parity slice: `/settings/image-style`, captured in Blawgy screenshot evidence, now canonicalizes to the protected Image Settings panel with `section=image-style` so the screenshot-backed Include Images/Product Images/Visual Style controls appear first, while `/settings/images` and `/settings/image-settings` keep the generic Image Settings first fold.
- Sir Bloggsalot local dashboard API and render coverage through `npm run check`.

## Missing Blawgy Screenshot Pockets

These are still the highest-leverage missing captures for a 1:1 clone:

1. Content Plan with overlays closed, including table/list/calendar states and article row/card actions.
2. Opened article detail drawer/modal, including edit, schedule, generate/regenerate, publish/unpublish, delete, loading, validation, success, and failure states.
3. Article Builder after generation, including editor, SEO fields, image fields, preview, save, schedule, publish, and regenerate controls.
4. Topics with real results, filters, metrics, saved batches, selected rows, add-to-plan before/after, pagination, empty, loading, and provider-error states.
5. CMS provider forms and errors for WordPress, Webflow, Shopify, Wix, Ghost, and Custom API where available.
6. Products with at least one manual product, add/edit/detail controls, filters/search, inventory feed connected/sync/failure/disconnect states.
7. Image Settings after a test generation, including exact output card/thumbnail, prompt/history, provider failure, and reset behavior.
8. Billing with current plan, upgrades/downgrades, monthly/annual toggle, invoice detail, failed payment, cancel, reactivate, Stripe portal handoff, and Stripe Checkout/Link trial confirmation screens.
9. Search Console connected state, Rankings active Pro+ state, AI Mentions active state, Reports template/share/schedule/export, and SEO Analysis detail views.
10. Workspace switcher, add-site flow, Create/overflow menu labels, invites, role controls, member removal, and team activity.
11. Mobile account layouts, especially sidebar/nav, tabs, tables, Article Builder, Billing, and one long modal/form.

## Recommended Next Implementation Slice

Do the next build pass around Content Plan and Article Builder parity because that is the core daily user workflow and has the most screenshot evidence already:

1. Match Content Plan shell states first: overlay-free layout, Strategy button, grid/list/calendar controls, refresh, overflow menu, Add Topics, status counts, and row/card action affordances.
2. Tighten article detail/edit flows: use Blawgy labels/order once screenshot evidence arrives, keep existing local persistence, and keep unsafe publishing/provider behavior local until real integration evidence exists.
3. Tighten Article Builder generated state: the local generated editor shell, summary blocks, metadata side panels, preview, and Save/Schedule/Publish/Regenerate action feedback now have coverage; exact provider-generated body layout, template-specific fields, and Blawgy validation/error states still need screenshots.
4. Re-run render/action tests after every pocket and compare screenshots before production deploy.

## WFS Board Update

Target board:

`https://workflowshortcuts.com/?boardId=6a730c30a79c66da51798593`

Target columns when applicable:

`Progress Over 50%`

`Progress Over 90%`

Current access result:

- Anonymous `GET /api/boards/6a730c30a79c66da51798593` still returns `{"error":"Record not found"}` because the board is private.
- Hermes/WFS bridge config at `~/.hermes/wfs_bridge/wfs_bridge.yaml` confirmed the authenticated bot path: `POST /api/bot/actions` with `WFS_API_KEY` loaded from `~/.env` or Keychain.
- Authenticated board lookup verified board title `sirbloggsalot`, the column key for `Progress Over 50%` (`column-1786675759686-w74sll`), and the column key for `Progress Over 90%` (`column-1786676628143-gcnchg`).
- The bot action wrapper updates card body text, but card column moves require `PATCH /api/documents/:id` with `status` and `updateBoardProgress: true`.
- Cards verified in `Progress Over 90%` on 2026-08-14:
  - `Homepage and marketing 1:1 parity`
  - `Backend compatibility and persistence gaps`
  - `Audit welcome wizard and intro promises`
- Cards verified in `Progress Over 50%` on 2026-08-14:
  - `build like blawgy`
  - `Content Plan command center parity`
  - `Write and Article Builder parity`
  - `Settings buttons: backend-backed actions and readbacks`
  - `Track/reporting gates parity`
  - `Billing, trial, signup/login/account parity`
  - `Topics and keyword research parity`
  - `Products and source-provider flows parity`
- Cards verified in other columns on 2026-08-14:
  - `Capture fixtures and E2E parity evidence`: `Blocked`, because more authenticated Blawgy screenshots/fixtures are required before that card can be completed honestly.
  - `Business locations and Pages generator parity`: `Progress Over 50%`, because local Business Locations CRUD, detail dialogs, empty-state parity, route-compatible Pages aliases, local location-derived page target previews, and Article Builder seeding are implemented. Exact Blawgy Pages generator screenshots, batch controls, generated page results, and provider publishing remain open.
  - `Production-readiness/deploy checklist`: `Backlog`, because production readiness must wait for parity, mobile checks, safe deploy prep, and smoke testing.
  - `Phase 1 core: SPA on real session + real providers`: `Done / Shipped`.
  - `Phase 1 hardening pass (timeouts, concurrency caps, restart recovery)`: `Done / Shipped`.
- Board cleanup on 2026-08-14 renamed the old Products card to `Products and source-provider flows parity` and removed old provider-specific wording from card title/text.

Task card text posted/updated through authenticated WFS access:

```html
<b>SBA Blawgy 1:1 dashboard clone — capture gaps + next parity slice</b>
<div><br></div>
<b>WFS columns:</b> Progress Over 50% / Progress Over 90%
<div><br></div>
<b>Status:</b> In progress. Unwanted provider and industry-specific copy cleanup is complete and verified clean across local source, production public root, runtime state, and live pages. Sidebar/header parity is patched locally to the fuller Blawgy screenshot vocabulary: the visible sidebar now shows a <code>CREATE</code> group with <code>Content Plan</code>, <code>Write</code>, and <code>Topics</code>; a <code>TRACK</code> group with <code>Rankings</code> and <code>AI Mentions</code> Pro+ badges plus <code>Search Console</code>; then <code>Settings</code>, <code>Getting started</code>, <code>Billing</code>, <code>Help</code>, and <code>Log out</code>. Reports, SEO Analysis, and the old local Create dropdown remain route/action-compatible hidden local panels until Blawgy sidebar evidence shows where they belong. The walkthrough overlay now follows the Blawgy-style 11-step progress model with dot indicators, <code>1 of 11</code> copy, fixed modal/backdrop presentation, and screenshot-backed step sequence from the available captures while preserving SBA branding in visible copy. The Articles panel now uses the visible <code>Content Plan</code> heading, inline <code>scheduled · writing · published</code> stats, visible Strategy/grid/list/refresh/overflow/date-window/<code>+ Add topics</code> controls, and Blawgy grid-first calendar/card display behavior while preserving hidden Bulk Schedule and Article Builder compatibility actions. Row actions now match the Blawgy screenshot compact pattern: scheduled/published rows show View, Edit, Delete icons; generated/processing rows show Generate/Regenerate and Delete icons; extra local actions stay in the row More menu. The Article Builder panel now uses the visible <code>Article Builder</code> heading and <code>Generate article</code> primary action while preserving the local save/preview/generate flow; generated or opened drafts now reveal a local Save/Schedule/Publish/Regenerate action rail wired to the protected article schedule, publish, and generate endpoints when a source article exists, and the editor now uses a Blawgy-style generated draft shell with readiness, schedule, public URL, SEO, and featured-image panels around the draft body. The Keyword Finder panel now uses the visible <code>Keyword Finder</code> heading, high-value-keyword subtitle, <code>keywords found</code> summary, and Blawgy grouped filter-card layout with DIFFICULTY (KD), CPC ($), VOLUME, and COMPETITION Min/Max controls while preserving keyword search/filter/save internals. Site Settings now follows the Blawgy first-fold order with Product Description, Target Audience helper copy, Your keywords helper copy, and Keyword mix helper copy before advanced SBA fields. The Products disconnected state now shows a single neutral source card with only <code>+ Connect</code>, a Blawgy-style storefront line icon, visible <code>+ Add Product</code>, filters/search, and the <code>No products yet</code> / <code>Add your first product</code> empty state while Sync/Disconnect and disconnected source details stay hidden until a source is connected. The Google Search Console panel now uses the visible <code>Google Search Console</code> heading and disconnected status copy while preserving Google provider wiring; its connected header now narrows to the date-range selector plus an icon-only Disconnect action, with Sync/Export kept as hidden compatibility handlers.
<div><br></div>
<b>Current evidence:</b>
<ul>
  <li>Current source: <code>/Users/bradmin/Documents/sirbloggsalot.com</code></li>
  <li>Current missing capture doc: <code>docs/blawgy-missing-screenshot-pockets-2026-08-14.md</code></li>
  <li>Current progress doc: <code>docs/blawgy-clone-progress-2026-08-14.md</code></li>
</ul>
<b>Need screenshots next:</b>
<ol>
  <li>Content Plan overlay-free plus article actions/detail drawer</li>
  <li>Article Builder generated editor</li>
  <li>Topics real results/add-to-plan</li>
  <li>CMS provider forms/errors</li>
  <li>Products populated plus inventory feed states</li>
  <li>Image Settings generated output</li>
  <li>Billing invoice/failure/cancel/reactivate</li>
  <li>Tracking/reports active states</li>
  <li>Team/workspace switcher</li>
  <li>Mobile long modal/form</li>
</ol>
<b>Next implementation slice:</b> article detail/drawer parity and exact generated Article Builder provider/template states once screenshots are available, then run <code>npm run check</code> and screenshot compare before any production deploy.
```
