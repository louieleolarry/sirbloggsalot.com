# Blawgy Dashboard Capture And Sir Bloggsalot Account Build Brief

Date: 2026-08-14

## Purpose

Sir Bloggsalot now has working Google login and a persisted `/account` dashboard with local implementations for the major controls. The shell intentionally borrowed the Blawgy dashboard structure. This brief is for a local LLM or operator with access to the real Blawgy dashboard so they can capture the remaining product behavior, compare it to Sir Bloggsalot, and hand back screenshots, endpoint notes, and build instructions without overwriting unrelated production work.

For a shorter copy/paste handoff, use `docs/blawgy-local-llm-capture-instructions.md`. For the current missing screenshot pockets, use `docs/blawgy-missing-screenshot-pockets-2026-08-19.md`; it supersedes the older scattered screenshot-request notes. For the current progress/WFS board update packet, use `docs/blawgy-clone-progress-2026-08-14.md`. This file remains the fuller engineering reference.

## Current Source Of Truth

- Treat `/Users/bradmin/Documents/sirbloggsalot.com` as the current dashboard build source for this handoff.
- A narrow production deploy on 2026-08-14 copied only `index.html`, `app.js`, and `server.js` from this checkout to `quicksites-prod:/var/www/sirbloggsalot.com/html`, after backing up the prior remote copies at `/home/ec2-user/sirbloggsalot-backups/deploy-20260814T011042Z`. Post-deploy checks showed `https://sirbloggsalot.com/api/health` returning `{"ok":true,"service":"sirbloggsalot","googleAuthEnabled":true}`, `/`, `/login`, and `/signup` returning 200, `/account` redirecting anonymous users to `/login?next=%2Faccount`, and dashboard aliases such as `/dashboard`, `/article-builder`, `/keyword-finder`, `/reports`, `/search-console`, `/rankings`, `/ai-mentions`, `/seo-analysis`, `/help`, and `/billing` redirecting to protected account views. Production is now a deployed runtime target, but this local checkout remains the engineering source of truth because it contains tests, docs, and handoff artifacts that are not copied to production.
- Local public-route smoke coverage now follows every case-study URL linked from `index.html` instead of checking only one hardcoded slug, so broken linked case-study routes should fail `npm run check`.
- Before any future deploy, compare remote files, back them up, and ship only the intended changed files. Verify `/api/health`, anonymous `/account`, authenticated `/account`, `/signup`, and the cache-busted `/app.js` and `/styles.css` assets after deploy.

## Screenshot Pockets Needed Next

Ask the Blawgy-side local LLM for these first. The current detailed request lives in `docs/blawgy-missing-screenshot-pockets-2026-08-19.md`. Each pocket should include a desktop screenshot, the route or click path, and a short note describing required fields, success/error/loading states, and safe redacted network endpoint shapes when visible.

1. `article-builder-generated-editor.png`: generated Write / Article Builder state with body editor, SEO fields, image fields, preview/readiness panel, save, schedule, publish, regenerate, and validation/error copy.
2. `content-plan-article-drawer.png`: opened Content Plan or Articles detail row with lifecycle actions, publish/schedule confirmation copy, delete/unpublish behavior, loading, success, and failure states.
3. `settings-cms-provider-error.png`: Settings -> CMS Connect provider-specific fields, exact validation copy, test result panel, draft-first/auto-publish controls, and collection/blog picker if visible.
4. `billing-plan-invoice-failure.png`: Billing plan change, invoice detail, cancel/reactivate, failed-payment state, and Stripe portal/checkout/link handoff with payment data redacted.
5. `tracking-reports-active-states.png`: Search Console connected state, Rankings active Pro+ state, AI Mentions active state, Reports share/schedule/export, and SEO Analysis issue/detail views.
6. `products-inventory-sync-states.png`: Settings -> Products with one manual product, product add/edit form, inventory feed connect/sync/disconnect/failure states, exact product fields, card/list layout, and synced-product metadata.
7. `image-settings-generated-output.png`: Settings -> Image Settings and `/settings/image-style` after a safe preview, including generated thumbnail/result state, failure state, history rows, preset labels, and asset/product-image behavior.
8. `team-workspace-switcher-open.png`: workspace/site selector open, Create menu open, team invite/member controls, add-site flow, invite rows, revoke/remove/role controls, and team activity layout.
9. `mobile-account-long-modal.png`: phone-width dashboard with one long modal/form open, preferably article edit, CMS connect, support ticket, report schedule, or invoice detail.

## Operator Rules For The Local LLM

- Work from a fresh copy or clean branch. Do not overwrite uncommitted Sir Bloggsalot changes.
- Redact all secrets, tokens, passwords, billing details, private addresses, and private customer data before sharing screenshots or notes.
- Prefer screenshots plus short factual notes. Do not paraphrase secret values or paste credentials.
- For each captured Blawgy action, note the route, visible state, click path, form fields, required fields, success state, failure state, and any DevTools endpoint/payload shape if safe.
- Stop before submitting any real external integration that would change billing, publish content, revoke credentials, send email, sync a live provider, or invite a real user.
- When comparing SBA to Blawgy, mark each item as `matches`, `partially matches`, `missing`, or `intentionally local placeholder`.

## Copy/Paste Prompt For The Local LLM

Use this prompt with a local LLM or operator that can access the real Blawgy dashboard:

```text
You are comparing the authenticated Blawgy dashboard to the local Sir Bloggsalot account dashboard. Your job is not to implement code. Capture evidence and return a build packet that another engineer can use safely.

Rules:
- Redact secrets, tokens, passwords, cookies, billing/payment details, private addresses, private customer data, and real API keys.
- Do not submit any action that publishes content, sends email, changes billing, revokes live credentials, syncs a live provider, or invites a real user.
- Prefer overlay-free desktop screenshots at 1440px wide, plus mobile screenshots for the specified mobile pockets.
- For every action you inspect, record route, click path, visible state before/after, fields, validation errors, success/error copy, loading state, and safe DevTools endpoint/method/request keys/response keys when available.

Priority capture order:
1. Content Plan: overlay-free dashboard, Strategy, Bulk Schedule, Add topics, article drawer/card edit, publish, schedule, generate/regenerate, grid/list, status filters, loading/error/empty states.
2. Write/Editor: default page, template/type chooser, brief fields, generate states, generated editor, SEO/image metadata, preview, save, publish, schedule, regenerate.
3. Topics/Keyword Finder: search filters, results metrics, topic detail, add-to-plan before/after, no-results/loading/error states, saved keyword batches.
4. Settings: Products add/edit/inventory feed, Image Settings test output/failure, CMS platform forms/success/failure/disconnect, Locations, CTA edit/validation, Invite Users/team roles/revoke/remove.
5. Tracking/Reporting: Search Console disconnected and connected dashboards, Rankings Pro+ gate and active state, AI Mentions Pro+ gate and active state, Reports, SEO Analysis.
6. Billing/Account/Nav: in-app Billing, plan changes, cancel/reactivate, invoice/payment failure states, workspace switcher and multi-site flow, Help/chat, logged-in public nav, signup/login/account behavior.
7. Mobile: sidebar, settings tabs, content plan, write editor, billing, and modals.

Return a folder or zip with README.md, screenshots/, routes.md, fields.md, network.md, comparison.md, and build-next.md. Use the comparison checklist in docs/blawgy-dashboard-handoff.md and mark each area as matches, partially matches, missing, or intentionally local placeholder.
```

## Highest Priority Capture Pockets

These are the screenshots I would ask for next, in order:

1. Content Plan without onboarding overlays, plus article detail/edit/publish states.
2. Write page through generated draft/editor/preview states.
3. Topics search/discovery results, filters, add-to-plan state, and error/empty states.
4. Products after at least one manual product exists, plus Add/Edit Product and Inventory feed connection screens.
5. Image Settings test-generation output, style presets, guideline editor, and failure states.
6. CMS platform-specific forms and success/failure/disconnect states.
7. Billing page inside the app, including upgrade/downgrade/cancel/payment failure/invoice states, plus the external Stripe Checkout/Link trial confirmation path.
8. Search Console connected dashboard, Rankings active Pro+ state, and AI Mentions active Pro+ state.
9. Workspace switcher opened, multi-site add/switch flow, member role controls, and invite revoke/remove flows.
10. Mobile layouts for sidebar, settings tabs, content plan, write editor, billing, and modals.

## Known Attached Blawgy Screenshot Evidence

Use these prior attachments as partial reference material, but do not treat them as a complete capture packet. They show useful layout and copy, yet many are tour overlays or public marketing pages rather than final dashboard states.

- `image-1.png` and `image-3.png`: public Blawgy marketing/homepage captures, useful only for public nav, promo banner, hero copy, and auth CTA comparison.
- `image-10.png`: authenticated `/settings/products` empty Products screen with Inventory Feed card, product filters, product search, and Add Product button.
- `image-11.png`: cropped authenticated dashboard sidebar showing workspace switcher, primary navigation, Pro+ pills, Billing, Help, and Log out.
- `image-13.png`: authenticated `/settings/site-settings` screen with Product Description, Target Audience, keywords, keyword mix, and Generate using AI control.
- `image-14.png`: final onboarding-tour modal over Site Settings, useful for walkthrough copy, progress dots, and finish-state behavior.
- `image-15.png`: onboarding-tour modal pointing at Image Settings, useful for settings-tab tour behavior and modal positioning.
- `image-25.png`: authenticated `/dashboard` Content Plan with welcome-tour modal over the calendar/cards.
- `image-26.png` and `image-27.png`: `/success` post-checkout states with free-trial confirmation, content-plan preparation progress, and `Open your content plan` CTA.
- `image-28.png` and `image-29.png`: external Stripe Checkout and Link trial confirmation screens for Blawgy Pro, useful for billing-provider handoff comparison only. Redact payment and identity details before sharing derived captures.

Still request fresh, overlay-free Blawgy captures for article detail drawers, Write/editor states, Topics results, product add/edit dialogs, CMS connection failures, Billing upgrade/cancel/invoice flows, Search Console/Rankings/AI Mentions active data, Reports, Help/chat, and mobile layouts. Mobile captures should include at least one long modal/form state, such as CMS connect, support ticket create, article detail edit, report schedule, or invoice detail.

## Current Sir Bloggsalot State

- Google sign-in works and redirects an authenticated user to `/account`.
- `/account` is server-protected: anonymous requests redirect to `/login?next=/account`.
- Blawgy-style dashboard aliases now canonicalize into the existing account shell: `/dashboard`, `/content-plan`, and `/articles` go to `/account?view=plan`, `/write` and `/article-builder` go to Article Builder, `/topics` and `/keyword-finder` go to Keyword Finder, `/settings/...` paths such as `/settings/site-settings`, `/settings/products`, `/settings/images`, `/settings/image-settings`, `/settings/image-style`, `/settings/cms`, `/settings/cms-connect`, `/settings/locations`, `/settings/business-locations`, `/settings/call-to-action`, `/settings/cta`, `/settings/invite-users`, and `/settings/invite` go to the matching Settings tab, and direct dashboard-section paths such as `/rankings`, `/search-console`, `/google-search-console`, `/ai-mentions`, `/reports`, `/seo-analysis`, `/onboarding`, `/tours`, `/getting-started`, `/subscribe`, `/upgrade`, `/billing`, and `/help` go to their account panels. Exact `/reports` now canonicalizes to `/account?view=reports`, while `/reports/:shareId` remains reserved for public shared report pages.
- The dashboard has Blawgy screenshot vocabulary in the visible left navigation: a `CREATE` group with Content Plan, Write, and Topics; a `TRACK` group with Rankings, Search Console, and AI Mentions; then Settings, Getting started, Billing, Help, and Log out. Rankings and AI Mentions carry the visible Pro+ pills, the visible sidebar uses a shared SVG line-icon sprite instead of local text glyphs, and Getting started exposes the guided-walkthrough tooltip copy shown in the sidebar capture. The account sidebar brand mark preserves the SBA `S` while matching the Blawgy mint speech-bubble silhouette. Reports and SEO Analysis remain route-accessible hidden local panels until Blawgy sidebar evidence shows their placement. The old local Create dropdown remains hidden compatibility markup, while its New article, Add topic, Add product, Add location, and Create ticket actions remain routed into existing local dashboard flows. The hidden Create > New article path and the hidden Content Plan `Article Builder` compatibility action both open a fresh Article Builder draft through the existing `article-builder` action route and clear any previously opened article source id so a new draft cannot accidentally update an old row. The account brand/home link explicitly targets `/account?view=plan` so it returns to the Content Plan home view instead of restoring the last saved dashboard panel. The Content Plan item still uses the internal `plan` view key for compatibility, Write still uses the internal `write` view key, Topics still uses the internal `topics` view key, Billing still uses the internal `billing` view while `/subscribe` and `/upgrade` stay compatibility aliases, and the internal `getting-started` panel remains route-accessible through `/onboarding`, `/tours`, and `/getting-started` while also appearing as the visible Getting started item.
- Settings has tabs for Site Settings, Products, Image Settings, CMS Connect, Business Locations, Call to Action, Invite Users, and Save Changes. Site Settings now follows the Blawgy screenshot's first-fold sequence: Product Description, Target Audience helper copy, Your keywords helper copy with the side-by-side keyword textarea/Add row, Keyword mix helper copy, then the additional advanced SBA fields below that visible sequence. Image Settings now follows the Blawgy first fold with the collapsed Test Image Generation card, In-Article Images heading/subtitle, In-Article Image Style card, reuse-style checkbox row, In-Article Image Guidelines card, default-guidelines row, and critical-guidelines copy while local image action/history wiring stays hidden for compatibility until generated-output screenshots are captured; the `/settings/image-style` variant now also uses the screenshot-backed Image Guidelines helper copy about adding specific constraints for what should or should not appear in generated images. CMS Connect now follows the Blawgy sparse first fold with only Website URL and Website Platform visible while local CMS action/status/detail wiring stays hidden for compatibility until provider-form screenshots are captured. Business Locations now follows the Blawgy empty-state first fold with Pages-generator-and-Write explanatory copy, a centered bordered empty card, generated-pages-and-articles helper copy, and `+ Add a location` CTA. The Blawgy-referenced Pages tab is now route-compatible locally through `/pages`, `/page-generator`, and `/pages-generator`, canonicalizing to a protected hidden-nav `Pages` account panel that renders location-derived page targets and can seed Article Builder with a local landing-page brief. Call to Action now follows the Blawgy sparse first fold with the left enable toggle, `Call to Action` heading, right-aligned `Edit CTA`, and the four-line CTA Best Practices card while retaining hidden local field/preview/reset compatibility behind the Edit CTA modal. Invite Users now follows the Blawgy first-fold structure with a single Email Address row, disabled-until-email `Generate Invite Link` button, manual-send helper copy, Current Team Members, and expanded invitation-link instructions while retaining the hidden local role selector for invite API compatibility; empty accepted-member, pending-invite, and Team Activity blocks stay hidden until data exists so they do not interrupt the captured first-fold layout.
- Image Settings image-style controls parity slice: the `/settings/image-style` section exposes the screenshot-backed Include Images in Articles, Product Images, Visual Style, and Image guidelines controls through the existing local `images.*` bindings while keeping preset/aspect/cadence and prompt-history actions as hidden compatibility controls until deeper generated-output captures arrive.
- Image Settings route-alias parity slice: `/settings/image-style` canonicalizes to the protected Image Settings panel with `section=image-style`, matching the captured Blawgy route while preserving `/settings/images` and `/settings/image-settings` as the generic Image Settings aliases.
- Image Settings route split parity slice: the screenshot-backed image-style controls render only when the protected account URL carries `section=image-style`, so `/settings/image-style` opens directly to that section while `/settings/images` and `/settings/image-settings` keep the separate generic Image Settings first fold.
- Image Settings image-style tools/toggle parity slice: `/settings/image-style` includes compact Visual Style reset/suggestion utility buttons and a persisted `Use custom guidelines` toggle that keeps the guidelines editor hidden until custom constraints are enabled.
- Image Settings product-image empty-state parity slice: `/settings/image-style` now forces the Product Images toggle off and disabled when the account has no products, matching the captured empty Product Images state while re-enabling the saved preference after products exist.
- Business Locations empty-state shield-icon parity slice: the empty Business Locations card uses the screenshot-backed mint circular shield/check SVG treatment in static markup and dynamic no-location renders instead of the older placeholder glyph.
- Invite Users pending-invite avatar parity slice: pending invite rows now reuse the Blawgy-style circular initial chip and compact member-row grid before the invite email/status/details, while preserving local detail, copy-link, and revoke actions.
- Account name, email, avatar, role, team owner email, and workspace label still come from the authenticated Google session and selected workspace. The account metadata topbar remains in hidden DOM for state plumbing, but it is not visible in the primary dashboard shell so page views begin directly with their Blawgy-style heading/subtitle.
- Client-side tab switching works.
- Account data now persists in an authenticated JSON-backed account store, scoped by signed-in user id.
- Settings save persists Site Settings, Image Settings, CMS setup metadata, CTA settings, and the Write draft. Site Settings now include description, target audience, brand voice, competitors, language, publishing cadence, timezone, default publish time, keywords, and validated keyword mix. Language, publishing cadence, timezone, image style preset, CMS platform, CTA placement, and CTA style are now validated against the dashboard's fixed select options, and partial settings saves preserve omitted neighboring fields instead of clearing them. Global Save Changes now consumes the full returned account snapshots from the settings and write-draft saves, so setup progress and derived dashboard state repaint when business basics become complete. Inline Add Keywords, saved keyword removal, Reset CTA, and Edit CTA settings mutations also consume returned account snapshots before repainting, so setup progress and dependent account panels do not stay stale after those quick settings actions. Actions that depend on a successful settings save, including Generate Description, Test Image Prompt, Connect CMS, and Test CMS, now stop after a failed save instead of continuing to the next backend action with stale settings. The visible Add Keywords, saved keyword removal, and Generate Description actions are covered through the client render/action harness, and Generate Description now exposes covered in-panel progress feedback while it saves/generates. Article Builder uses the saved default publish time when a draft has no explicit scheduled time. CTA settings now include enabled/label/text/url/placement/style/open behavior/tracking label with web URL validation, a live preview card, a local CTA detail dialog with Edit CTA, a dedicated local Edit CTA modal, and a covered local reset action that is disabled when the CTA is already empty/default.
- Generate using AI for Product Description now runs through a local account API, shows an in-panel local progress state, and saves a generated starter description from audience, keywords, products, and locations.
- Image Settings now has a covered local test action with in-panel progress feedback while it saves settings and generates the latest image prompt preview from style preset, aspect ratio, image cadence, visual style, image guidelines, content plan, and product-image preferences. The result renders a local preview card with compact local/provider-pending/error status badges, provider errors show `Needs attention` plus the saved failure reason, and Clear history exposes covered in-panel progress feedback, consumes returned account snapshots, and repaints setup/dependent panels so setup progress does not stay stale after prompt history is cleared. Legacy stored image settings using `includeProductImages` or `customGuidelines` are normalized into the active `useProductImages` and `guidelines` fields when account data is loaded, and malformed stored image preview rows are dropped.
- Articles now renders from stored data with Blawgy-style visible labels, table filters with live badges for scheduled, processing, generated, published, needs-attention, and paused states, and a selectable table shell with title/keyword/date/actions columns. The visible first fold now matches the dashboard screenshot more tightly: the header says `Content Plan`, shows inline `scheduled · writing · published` stats, uses the screenshot/static-fallback summary copy about articles publishing themselves and new topics being added weekly, shows the `Why this plan` card by default with dismiss support, uses the screenshot-backed strategy fallback phrasing for keyword research topics, posting spacing, and rough monthly visit range, and exposes Strategy with the screenshot-backed sliders icon plus icon-only grid, list, refresh, and overflow controls, date-window controls, and `+ Add topics`, while Bulk Schedule and Article Builder remain wired as hidden compatibility actions until exact overflow/menu placement is captured. Calendar cards now include the screenshot-backed footer row with a standalone date pill and `Added by Sir Bloggsalot` source label, preserving allowed SBA branding in place of Blawgy branding, display Blawgy's `Easy to rank` card wording for locally stored `Easy` difficulty values without changing saved data, render projected visits as a dedicated green emphasis line, and open the same protected `Article detail` modal as table-row View instead of a separate legacy edit form. Covered bulk selected-row status/clear actions across every local article status stay hidden at zero selected rows, then expose the selected count, status selector, Apply status, and Clear controls when at least one article row is selected. The Blawgy screenshot calendar/card grid now renders by default before account data loads and for new accounts. Grid/list mode persists through visible controls, Previous/Today/Next moves a three-week calendar window, covered Bulk schedule creates multiple scheduled articles from unused topics and validates count/frequency/status/date ranges, Add topics opens a topic form and schedules it immediately, Start article saves the Write draft first and creates the plan item from that normalized saved draft/preview with a supported local difficulty value, article titles are required, and covered Refresh reloads account data without reporting success on failed loads. Strategy saves and article lifecycle actions now consume returned account snapshots, so setup progress and derived panels do not stay stale after a content-plan mutation. Read-only workspace members can inspect Strategy in a view-only dialog without receiving a write-capable submit handler. Legacy stored content-plan rows are normalized on account load, so malformed article dates/times/numbers/statuses/URLs and external stored public paths do not break dashboard rendering or report/SEO links.
- Article detail modal parity slice: article rows and calendar cards now fetch a protected single-article detail record and can open a structured in-app article detail modal, the metadata editor dialog, schedule through a row-level modal, confirm publish/unpublish, generate/regenerate drafts, delete rows, or load the article into the Article Builder. The article detail modal shows status, excerpt, readiness/missing-field guidance, metadata, SEO/canonical/public-path fields, draft body, and the existing Edit, Open in Article Builder, Schedule, Generate/Regenerate, Publish/Unpublish, and Delete lifecycle actions through the protected blog action wiring. It mirrors protected publish blockers for missing title, slug, body, or future schedule by disabling Publish and showing the backend-aligned reason; row-level Publish uses the same blocker before opening confirmation. Its action-state panel now opens an Article lifecycle detail dialog with title, status, schedule, public URL, CMS/publish mode, readiness, blockers, generation/update state, issue state, SEO, body, protected-request progress, and the remaining target screenshot evidence gap. Publish, unpublish, and delete confirmations now include an action-readiness panel naming the protected account request, visible progress expectation, account status-line failure surface, and local account refresh result so the detail lifecycle path has explicit loading/success/failure affordance copy. Direct Open in Article Builder and Generate/Regenerate from the detail modal now switch/refresh state and close the modal so stale article details are not left onscreen. The visible row action cell now follows the Blawgy screenshot compact pattern instead of exposing every workflow inline: scheduled/published rows show View article, Edit article, and red Delete article icon actions; generated/processing rows show Generate/Regenerate and red Delete article icon actions. Extra local workflow actions remain covered in the row More menu for View article, Edit article, Open article builder, Schedule, Generate/Regenerate, Publish/Unpublish, and Delete while retaining the same read-only protections. Generated rows show a Regenerate control instead of the initial Generate label. The edit dialog exposes title, slug, keyword, category, excerpt, canonical URL, author name, SEO title, meta description, internal links, schema type, featured image metadata, draft body, scheduled date/time, status, search volume, difficulty, estimated visits, and notes, plus generate, publish, save, and delete actions. Protected article create/update now preserves internal links, schema type, canonical URL, author, volume, difficulty, and estimated-visit metadata so SEO Analysis and report checks do not lose those fields after an edit. The Article Builder handoff fills the richer builder fields from the stored article, including source article id, slug, category, excerpt, canonical URL, author name, schedule, template, audience, word count, search volume, difficulty, estimated visits, SEO fields, internal links, featured image metadata, brief, body, notes, and local status. Exact Blawgy opened-draft screen fidelity, row-menu labels/order beyond the visible screenshot, and publish-state modal copy still need screenshot comparison.
- A protected `/api/account/blog/posts` editor facade now exists on top of the same content-plan article records. It supports authenticated list/detail/create/update/generate/schedule/publish/unpublish/soft-delete actions, status filtering, required target keywords for new editor posts, unique slugs including duplicate-title collision handling, public metadata fields, and exact-depth route rejection. Public blog routes still expose only sanitized published posts from the configured public publisher.
- The Article Builder panel now uses the Blawgy screenshot visible `Article Builder` page heading and `Generate article` primary action while keeping the existing internal `write` route and `start-article` action for compatibility. The underlying Article Builder workflow stores title, slug, keyword, category, excerpt, canonical URL, author name, schedule date/time, validated template, audience, validated target word count, search volume, difficulty, estimated visits, internal links, SEO title, meta description, featured image metadata, brief, draft body, private notes, source article id, source article status kept separate from the validated local builder workflow status, local readiness score/missing-field metadata, and a generated local brief/search preview with provider-pending/source metadata. It includes covered Article Builder navigation, Preview Brief, Save draft, and Generate article actions; Save, Preview, and Generate expose a covered in-flight progress line before clearing it on completion or failure. Generated or opened drafts now reveal an in-context Save draft, Schedule, Publish, and Regenerate action rail; Schedule, Publish, and Regenerate stay disabled until the builder has a source article id, then reuse the protected blog editor schedule, publish, and generate endpoints while returning the operator to Article Builder. Article Builder generated editor shell parity slice: the editor now presents a generated draft shell with readiness, schedule, public URL, SEO, and featured-image panels around the draft body while preserving every existing `data-write-field` binding. Generate article stops client-side before saving or creating a post unless title and target keyword are present, carries canonical URL, author, search-volume metrics, difficulty, estimated visits, internal links, and the opened source article's current status into the protected article record, Save draft consumes returned account snapshots so setup progress and derived dashboard state repaint after draft basics are saved, Save draft updates the original protected blog editor post when the builder was opened from an article row while staying in Article Builder without forcing scheduled/published articles back to draft, Generate article updates that same opened post or creates a new post otherwise, and existing article rows can be opened back into the builder for continued editing. The Create menu's New article path and the Scheduled Articles header `Article Builder` button reset the builder to a fresh draft while preserving the existing `article-builder` action route, while row-level Open article builder keeps the source article id for intentional edits. Canonical and featured-image URLs are both explicitly clearable from the saved write draft instead of falling back to stale values when the user blanks those fields. Legacy stored Article Builder drafts are normalized on account load, so malformed schedule values, unsupported templates/statuses, unsupported source statuses, unsupported preview metadata, out-of-range word counts, invalid canonical/featured image URLs, and unsafe featured image URLs do not block the dashboard from loading. Provider-generated long-form body fidelity, template-specific generated fields, and exact Blawgy validation/error states remain comparison work.
- Keyword Finder now uses the Blawgy screenshot visible `Keyword Finder` page heading, `Find high-value keywords...` subtitle, and `keywords found` summary while preserving the internal keyword search/filter/save data model and `/keyword-finder` compatibility route. The page includes visible `found` count above the filters plus `visible of total keywords` and selected-count footer inside the filter card, a Blawgy-style Filters header with Select All, rows include volume, CPC, difficulty and competition metric bars with numeric values, selection state, metric filters, and covered grouped min/max filter controls under the captured headings `DIFFICULTY (KD)`, `CPC ($)`, `VOLUME`, and `COMPETITION`. The visible search input was removed from the filter card to match the capture; backend-backed local Search compatibility remains available through the hidden local action/test harness and displays filtered results without replacing saved topic inventory. The page keeps Magic Select, downloadable CSV export, Save Keywords into Site Settings, keyword-detail dialogs, and covered add/edit/delete/add-to-plan actions. Local keyword search now exposes covered in-panel progress feedback while the search request is running. Header actions now match the Blawgy capture by showing Magic Select, Export, and Save Keywords while keeping local Search/Add topic controls hidden for compatibility. The row action column is also hidden to match the Blawgy table while keeping edit/add/delete controls in the DOM for local compatibility. Topic keyword cells now open a local detail dialog with keyword, title, volume, CPC, difficulty, competition, plan status, and an Add-to-plan action when allowed. Magic Select now operates on the currently visible topic source, including transient search results, and is disabled when no keywords are visible; Select All is disabled when filters leave no visible keywords; Save Keywords is disabled until at least one keyword is selected. Save Keywords sends selected transient search-result keyword text as well as selected ids; Edit on a transient search-result row updates the current search result locally; Add on a transient search-result row first persists that topic, then adds the created topic to the content plan; and Delete on a transient search-result row removes it from the current search results without calling persisted-topic APIs. Locally searched rows can therefore be selected, edited, saved, scheduled, or removed even before they originally existed as account topics. Manual topic ideas still require titles, expose the full local difficulty option set, and now include covered local metric entry for volume, CPC, numeric difficulty score, and competition on both add and edit forms, matching the metrics rendered in rows/detail dialogs. They can be deleted, reject unsupported row actions or repeated deletes, and show a distinct no-topic empty state instead of filter-copy after the last topic is removed. Persisted topic edit/delete actions now consume returned account snapshots and repaint setup/derived dashboard state, so setup progress does not stay stale after the topic inventory changes. Legacy stored topic rows are normalized on account load, so invalid difficulties/numbers and blank topic rows do not block the dashboard. Real keyword-provider data, pagination, saved keyword batches, provider errors, and Blawgy's full discovery/detail UX remain integration/comparison work.
- Products can be added and edited through in-app forms, require product name/category, validate optional product URLs, save URL/description/price/SKU/audience/featured/source/visibility metadata, hidden/shown, filtered, searched, and deleted through an in-app confirmation flow. Add Product now includes the same visible/hidden visibility choice as Edit Product, so manual products can be created hidden instead of requiring a follow-up hide action. Product rows now open a local detail dialog with name, category, price, SKU, audience, source, visibility, URL, description, and an Edit product action when allowed, and populated rows now carry compact source, featured, and visible/hidden badges for scan-friendly catalog review. Product filters and search now distinguish true empty inventory from no matching rows. Manual product add, detail, edit, hide/show, and delete row actions are covered through the client render/action harness. Product row mutations consume the returned account snapshot and repaint derived dashboard state, so setup progress and related account panels do not stay stale after a product is changed or deleted. Legacy stored products are normalized on account load so malformed stored URLs are cleared and old truthy/falsey visibility metadata is converted into safe booleans without weakening new product validation.
- Business Locations can be added and edited through in-app forms, require location name and city, validate 2-letter state codes, save address/phone/service area/primary-location metadata, render those details in saved cards, and delete through an in-app confirmation flow. Location rows now open a local detail dialog with name, city/state, address, phone, service area, primary/secondary status, and an Edit location action when allowed. Location add, detail, edit, and delete row actions are covered through the client render/action harness and now consume returned account snapshots so derived dashboard panels stay current after location changes. The local Pages panel also repaints from the same saved location state, showing three local page targets per location with Location detail and Draft page actions. Legacy stored locations are normalized on account load so invalid stored state names are cleared and old primary-location flags are converted into safe booleans without weakening new location validation.
- Invite Users can generate, display, inspect, copy, and revoke owner-scoped invite links with validated member/editor/admin role intent. Newly generated links now open an immediate clickable/copyable same-origin `/invite/:token` dialog in addition to rendering in the pending-invites row, so the operator does not have to hunt for the generated URL. Pending invite rows now open a local detail dialog with recipient, role, status, local invite URL, delivery copy, and a Copy link action when the invite URL is safe. Accepted member rows now include the Blawgy-style circular initial avatar and open a local detail dialog with name, email, role, status, and role-update audit metadata. Team Activity rows now open a local read-only detail dialog with event label, type, target, actor, role, and timestamp. Invite dialog copy and pending-row copy both reject external or stale non-local invite URLs. Existing invite detail/copy/revoke row actions and member detail/role/remove row actions are covered through the client render/action harness, and invite/member mutation rows now consume returned account snapshots so setup progress and derived account panels do not stay stale after team changes. Invite links now have a `/invite/:token` landing page, can be accepted by the invited signed-in Google account, accepted members render in the owner dashboard with remove support, local role labels, owner-side role update controls, role-update audit metadata, explicit empty states for no members/no pending invites, and a bounded Team Activity list for invite/member lifecycle events. Accepted users can switch between their own workspace and the owner workspace. The workspace switcher can now add another owner-scoped local site workspace with a validated optional website URL, immediately select it, and keep existing accepted/owned workspaces available. Removing a member drops that workspace from the removed member's accessible list, unsupported invite/member roles are rejected, repeated invite revoke/member remove attempts now return missing-resource errors, and legacy activity rows with malformed timestamps are dropped before they can render as `Invalid Date`.
- Account dialogs, report-share links, invite links, and export confirmations now stay inside the app shell or account live region; dashboard actions no longer fall back to browser `alert()` dialogs when dialog DOM is unavailable.
- Workspace role enforcement now blocks shared `member` users from mutating the owner workspace while preserving read access; owner/editor/admin roles can change workspace data. Rankings and AI Mentions GET endpoints now compute read-only snapshots without using the mutation gate, so member users can open those panels without a permission error.
- The account UI now mirrors that permission model for selected shared workspaces: top-level mutating actions, Settings save, Billing plan/period controls, member role changes, support status/reply actions, and row-level edit/delete/status controls in a `member` workspace stop client-side with a read-only role message instead of opening edit/create dialogs or sending mutation requests. View/tab/calendar browsing still works locally, but read-only workspaces do not persist those UI preferences back to the owner workspace. Invite link copy remains available because it does not mutate account state. Workspace labels and role checks now prefer the explicit selected workspace id over stale selected flags, so switching workspaces cannot show owner labels while enforcing member permissions. The workspace switcher remains available to members, but its nested Add site workspace action is hidden while the selected workspace is read-only. Read-only settings edits no longer mark the workspace dirty or enable the global Save Changes button, and Settings rendering disables fields, toggles, keyword chips, invite inputs, image history clear, image/CMS/inventory feed/CTA mutation buttons, and add/generate controls instead of leaving editable dead ends. Read-only product, location, member, invite revoke, Help create-ticket/help-bubble, and support row mutation controls now render disabled while non-mutating invite copy stays available. Read-only Billing rendering disables billing portal buttons, including the failed-payment alert portal button, plus plan, period, cancel, and reactivate controls while leaving invoice detail review available. Read-only Articles rendering disables Bulk schedule and Add topics while preserving Strategy, Refresh, calendar navigation, and Article Builder review/navigation; the table also clears any stale selected rows, disables select-all, row checkboxes, bulk status, bulk update, bulk clear, and row-level Edit/Schedule/Generate/Publish/Unpublish/Delete controls while leaving Open Builder available for read-only draft review. Read-only Topics rendering similarly clears stale keyword selections and disables Search, Add topic, Magic Select, Save Keywords, Select All, row checkboxes, and row-level Edit/Add/Delete controls. Read-only Article Builder rendering disables draft fields plus Save draft, Preview brief, and Start article while keeping the draft preview visible for review. Read-only Search Console rendering keeps connected snapshot browsing, filtering, ranges, and export available, but disables Connect, Sync, and Disconnect mutation controls. Read-only Reports rendering keeps report review and CSV export available, but disables Create share link and Schedule report because those mutate workspace report state.
- CMS Connect now stores platform, website URL, username/account identifier, status, last tested/connected timestamps, draft-first mode, and whether credentials are configured. The local Connect CMS dialog shows platform-specific labels and setup copy for WordPress, Webflow, Shopify, Wix, Ghost, and Custom API while still saving through the same safe metadata path, and now includes the local `Publishing mode` select for `Draft first` versus `Auto-publish after approval`. The provider checklist explicitly names the required credential for the selected provider and states that missing or rejected credentials stay visible as a provider error. Secret values can be submitted to mark credentials configured, but they are not stored, returned, or displayed. If the prerequisite settings save fails, the Connect CMS modal stays open and does not call the connect endpoint with stale settings. The visible CMS Connect, Test setup, and Disconnect actions are covered through the client render/action harness and now expose in-panel progress feedback while save/test/disconnect requests run. When no CMS metadata exists, Connect stays available and Test setup/Disconnect are disabled instead of appearing actionable. CMS detail rows now use compact connected/local-setup/error badges, and stored local provider/setup failures show a `Needs attention` badge plus the failure reason. CMS test requires a configured platform and URL, returns a local provider-pending result panel with platform, website, target, collection, mode, and credential status, and explicit blank, malformed, or unsupported website/platform submissions return validation errors instead of silently reconnecting from old values.
- Inventory feed connect now stores local retailer metadata, marks credential presence without returning secrets, validates retailer/account id, supports disconnect, and can sync placeholder inventory-sourced products into the Products list only after local connection. The disconnected Products view follows the Blawgy empty-state structure with one neutral source card and only the `+ Connect` action visible; Sync/Disconnect and source details stay hidden while the empty-state `+ Add Product`, All/Synced/Manual/Hidden filters, product search, and `No products yet` / `Add your first product` list remain visible. Stale hidden product filters/search reset to All/blank while that empty disconnected view is shown. The visible Connect, Sync, and Disconnect actions are covered through the client render/action harness with in-panel progress feedback, including synced product-row rendering and compact connected/disconnected status badges. The inventory state panel now opens a feed detail dialog covering feed status, account, sync mode, credential state, manual/synced/hidden product counts, last sync, latest SKU, issue state, connect/sync/disconnect action availability, provider-backed real API limits, and the remaining exact product add/edit plus synced-product detail screenshot gap. Local source failures now render a distinct `Needs attention` badge with the stored failure reason instead of burying provider problems in row text. Disconnected sync attempts still return validation errors instead of HTTP success if called directly. Legacy stored inventory feed state is normalized on account load, so incomplete connected metadata falls back to disconnected and cannot make the sync control appear ready incorrectly. Real inventory provider validation remains integration work.
- Image Settings now saves generated prompt test history with style preset, aspect ratio, and image cadence metadata, renders latest and prior prompts in the dashboard, and covers the visible Test image prompt and Clear history actions through the client render/action harness. Clear history is disabled when no prompt/history exists, while Test image prompt remains available. The image provider state panel now opens a structured detail dialog covering provider status, preview title, style preset, format/cadence, product-image inputs, prompt history, last test, alt text, prompt, issue state, test/clear action availability, provider-backed final generation/asset-library limits, and the remaining exact generated-thumbnail/failure-row/asset-library screenshot gap. Stored provider failures now surface as `Needs attention` with the failure reason; real generated image thumbnails, live provider failures, and asset-library selection remain integration work.
- Help now has local support tickets saved to the account workspace from the Help page or floating help button, validated setup/publishing/billing/account-access/bug categories, validated low/normal/high/urgent priorities, required ticket details, page/workflow context, local reply notes, plus resolve/reopen controls for local ticket status. Ticket rows now open a local detail/thread dialog with subject, status, category, priority, page context, requester, initial details, reply history, and a Reply action when allowed. The floating help button uses a Blawgy-style black circular chat-bubble icon while still opening the local support-ticket flow; it loads the account workspace before opening the ticket form when account data is not ready yet, and signed-out help attempts route to Google login with `next=/account?view=help`. The visible create ticket, detail, resolve/reopen, and reply controls are covered through the client render/action harness. Support list/create/update API responses now include the standard returned account snapshot, and support row actions consume it so setup progress and derived account state do not stay stale after a support mutation. Unsupported support categories, priorities, statuses, and empty ticket details are rejected for new submissions, while legacy stored tickets are normalized into safe dashboard status/category/priority values, malformed timestamps are cleared, and invalid stored reply authors fall back to the signed-in account when the account loads. Live chat and email delivery remain provider integration work.
- Report and invite copy-link controls now only show copied success after the Clipboard API write resolves. If the link is unsafe or the browser clipboard API is unavailable, the dashboard shows an explicit unavailable/could-not-copy state instead of silently claiming success.
- Shared account modal submissions now run native form validity checks before reading `FormData` or calling dashboard mutation handlers. Required fields and URL/number constraints therefore report through browser validation and do not post partial local workspace/settings/product/location/support mutations when the form is invalid.
- Account dialogs can close by close button, cancel action, backdrop click, Escape key, or dashboard route change, so long forms and confirmation modals are not mouse-only dead ends and do not remain stale over a newly selected view during dashboard review.
- Mobile account navigation now has a dashboard-specific Menu toggle. At phone width, the account brand and workspace switcher stay visible while the sidebar navigation groups collapse behind the toggle; tapping a dashboard destination, the account brand/home link, browser back/forward, or another dashboard route change closes the menu and preserves normal account view switching. Remaining mobile comparison should focus on Blawgy's exact sidebar animation, table density, settings tabs, and long-form modal layouts.
- Account views now support canonical and Blawgy-style query deep links such as `/account?view=article-builder`, `/account?view=keyword-finder`, `/account?view=reports`, `/account?view=tours`, and `/account?view=settings&tab=business-locations`, normalized to the internal account keys. The exact `/reports` route canonicalizes to the authenticated Reports dashboard, while generated shared-report URLs continue to live under `/reports/report_*`.
- Route checks now enforce dashboard view/panel parity, Settings tab/panel parity, full account view/settings tab switch coverage in the client harness, declared account-action handler coverage, visible button handler coverage, internal route coverage, generated dynamic row-control handler coverage, account browser back/forward tab restoration without UI-preference mutation, setup checklist jump fallback for stale or malformed view/tab targets, and mobile-safe account dialog viewport constraints so future click-around regressions are caught before deploy. A public server-route smoke now starts the Node app on an ephemeral local port and verifies public route HTTP statuses, signup trial copy, anonymous `/account` redirect, authenticated `/account?view=billing` shell rendering from an isolated temp session, empty public blog rendering, health/blog API JSON, and referenced PNG assets. Case-study routing now accepts only the known public slugs and sends unknown `/case-studies/*` paths to the existing not-found page instead of rendering a generic case-study fallback as if it were valid content. Direct requests for unknown app-shell paths now serve the client not-found shell with HTTP 404, while known public app routes keep HTTP 200. Malformed percent-encoded URL paths now return HTTP 400 instead of logging raw `URIError` exceptions as server errors. Malformed session cookies are ignored and treated as signed-out rather than crashing auth/session, account API, or account page requests. Dashboard-rendered public blog links and shared report HTML now accept only normalized one-segment `/blog/:slug` paths, preventing malformed stored values from becoming clickable broken/internal links.
- Articles now exposes and renders the current Blawgy screenshot `Content Plan` title, inline `scheduled · writing · published` stat pattern, visible Strategy, icon-only grid/list/refresh/overflow controls, rolling calendar controls, `+ Add topics`, default grid/card calendar, optional list-mode article table, Scheduled Date table header, Actions-before-Scheduled-Date table flow, and dynamic status counts for scheduled, writing, published, generated, processing, needs-attention, and paused items instead of relying only on static dashboard copy. Bulk Schedule and Article Builder remain hidden compatibility actions pending exact overflow/menu evidence. The status filter row now defaults to the Blawgy core tabs and only reveals Needs attention or Paused when those rows exist. The strategy card remains a local compatibility surface; selected-row bulk status controls now appear only after one or more rows are selected. Stored `draft` article statuses remain API-compatible, but the dashboard presents them as `Generated` in calendar cards, filters, and bulk status controls to match the Blawgy capture. Article cells still include the scheduled time when present.
- Content Plan grid/list toolbar state now syncs from saved account UI state during render, so restored list view does not leave the grid button visually active.
- Article cards now open an in-app edit form for title, keyword, date, status, volume, estimated visits, brief, draft body, SEO title, and meta description, with delete support.
- Article cards can generate a local draft from saved account settings, products, locations, CTA, and CMS state. Publish is local: the publish action only marks published when CMS is locally connected; otherwise it keeps the item as a draft and explains why. Content Plan row publishing, direct article creation, bulk schedule, bulk status updates, and both article edit endpoints now share the blog editor's publishability checks, so future-scheduled articles cannot be marked published through row actions, create requests, bulk actions, or edit-form status changes. Manual article edits and row-publish actions that set status to `published` now stamp publish metadata and stored local `publishedUrl` only after those publishability checks pass, published slug edits refresh that URL, non-published status changes clear it, and regenerating an already-published article preserves its published state plus local URL instead of silently moving it back to draft.
- Billing now renders persisted plan, status, billing period, payment method, portal state, local invoice history, failed-payment alerts, and safe invoice detail dialogs. Pro/Pro+ plan changes, covered monthly/annual switching, local checkout, local portal session, local payment-failed state, cancellation, reactivation, invoice-detail row clicks, and direct `/account?view=billing&invoice=...` invoice links are wired to account state. Billing checkout trial-start parity slice: local checkout records a 3-day trial end date and renders the captured renewal framing, such as `Then $79.00 per month starting August 17, 2026`, while still marking Stripe Checkout and Link payment confirmation as provider-integration work. Billing action rows now open a structured action-detail dialog covering plan/status, payment method, latest invoice, failed-payment reason, retry date, portal action, cancel/reactivate availability, provider-backed Stripe limits, and the remaining exact Blawgy billing-portal/cancellation copy gap. Invoice detail dialogs now render structured invoice metadata plus a payment-handoff panel that states Stripe invoice payment remains provider-backed and retry or payment-method changes happen in the billing portal. Billing portal, cancel, and reactivate actions now expose covered in-panel progress feedback while requests run. Billing period changes consume returned account snapshots, so setup progress and derived account panels repaint after monthly/annual changes instead of only refreshing the Billing panel. Failed-payment invoice rows now reuse the created invoice id in `hostedInvoiceUrl`, so generated invoice URLs resolve back to the protected invoice detail record instead of a different local id. Invoice deep links, including browser back/forward navigation to invoice URLs after account data is loaded, fetch the protected invoice detail, open the invoice dialog, and clear the stale `invoice` query parameter after handling. Missing or stale invoice ids now show the protected API error once and still clear the unusable `invoice` query parameter so the dashboard does not retry the same bad deep link on every account-route render. Cancel is disabled once the local plan is already cancelled, and Reactivate is disabled until cancellation, so impossible billing actions no longer appear clickable. The visible period toggle, portal, cancel, and reactivate actions are covered through the client render/action harness, with validation for unsupported local plan/period/status values. Legacy stored billing plans, periods, statuses, invoice rows, and blank workspace names are normalized on account load so invalid old values fall back to a safe Pro trial, malformed invoices are dropped before dashboard rendering/detail APIs, and Pro+ gated panels stay locked. The real Stripe portal and Stripe webhook/payment-method integration remain integration work.
- Google Search Console now uses the Blawgy screenshot visible `Google Search Console` page heading and disconnected status copy while preserving Google OAuth/Search Console provider wiring and `/google-search-console` compatibility. It has local connect validation, HTTP/HTTPS property URL validation, connected-only sync, disconnect, retained local metric values, 7/28/90-day local traffic trends, an inline clicks/impressions chart with Blawgy-style Clicks and Impressions legend chips, two Blawgy-style cards for Top Performing Posts and Top Search Queries with Page/Query, Clicks, and Impressions headers plus table-style click/impression rows, card-level row-count controls, row search, row-detail dialogs with query/page label plus clicks and impressions, and downloadable CSV export with trend rows. The separate metric-card row and older single-list section are hidden to match the Blawgy capture, but the values and compatibility list still update for reports/tests. The connected header now matches the visible screenshot pattern with only the date-range selector and an icon-only Disconnect action; Connect remains the disconnected-state action, and Sync/Export remain wired as hidden local compatibility handlers. Connect and Sync expose covered in-panel progress feedback, and the action harness still covers connect, sync, export, and disconnect, including sync fan-out into local Rankings and AI Mentions panels. While disconnected, Connect stays available and dependent controls such as Sync, Export, range, row-limit, search filter, and query/page toggles are disabled instead of appearing actionable. The connected subtitle displays the property without the protocol to match Blawgy while keeping the stored URL intact. Connected rows explicitly label the data as a local snapshot with Google OAuth/provider integration pending. Top pages link to public `/blog/:slug` article URLs. Blank or malformed property submissions and disconnected sync attempts return validation errors. Legacy stored Search Console state is normalized on account load, so malformed property URLs fall back to disconnected state, invalid sync timestamps are cleared before dashboard rendering, and invalid/external stored top-page rows are not rendered as dashboard links. No Google tokens are stored; real Search Console OAuth/API integration and provider data remain.
- Rankings now has a local Pro+ gated snapshot with tracked-keyword summary metrics, improved/declined filters, keyword search, 30/90-day date ranges, handled range-refresh errors, an average-position trend chart, row-detail dialogs with keyword, URL, position, change, date range, and provider-pending context, refresh with in-panel progress feedback, and downloadable CSV export with trend rows. Ranking rows link to public `/blog/:slug` article URLs. AI Mentions now has a local Pro+ gated snapshot with prompt summary metrics, mentioned/monitoring filters, source/model filters, 30/90-day date ranges, prompt/source/model search, a local mention-count chart, row-detail dialogs with source, model, prompt, status, date range, and provider-pending context, refresh with in-panel progress feedback, and downloadable CSV export with mention and trend rows. While either Pro+ panel is gated, dependent controls such as refresh, export, date range, filters, source/model selectors, and search are disabled instead of appearing actionable. Legacy stored Rankings and AI Mentions state is normalized on account load from normalized billing, so invalid ranges, malformed trend rows, unsupported mention statuses, empty prompts, and external ranking URLs cannot boot into broken dashboard state. Real rank tracking provider data, AI/source monitoring provider data, and richer export fidelity remain integration work.
- Reports now rolls up local content, Search Console clicks/impressions, Rankings, and AI Mentions into a single dashboard view with a local overview chart, row-detail dialogs with section, label, value, detail, public path, and local-rollup context, clickable local public-post links for report content rows in both the account dashboard and shared `/reports/:shareId` page, guarded local `/reports/report_*` share links and copy actions with in-panel progress feedback, local selectable templates for Performance summary, Content inventory, and Executive digest, configurable local weekly/monthly schedule metadata with validated recipients/template selection plus a schedule-dialog delivery setup checklist, local-only metadata status, provider-backed email-delivery limits, and in-panel save progress feedback, and downloadable CSV export including template metadata plus local public-post paths. Reports action rows now open a structured action-detail dialog covering template, Search Console, Rankings, AI Mentions, share link, schedule, last export, export/share/schedule action availability, provider-backed analytics/email limits, and the remaining exact Blawgy export/share/schedule modal copy gap. Tracking refresh and range changes now repaint Reports and SEO Analysis immediately, so connected Search Console, Rankings, and AI Mentions changes do not require a page refresh before related dashboard summaries update. Legacy stored Reports sharing, template, and schedule state is normalized on account load, so external share URLs, unsupported templates/cadences, invalid recipients, and malformed schedule/share dates do not boot into broken dashboard state. All dashboard CSV exports now neutralize spreadsheet formula-leading values before download. Scheduled email delivery, exact Blawgy chart/template fidelity, and richer export fidelity remain comparison/integration work.
- SEO Analysis now checks local article metadata, schema type metadata, internal-link presence, draft body readiness, published URL/crawl readiness, saved keywords, CMS state, and Search Console state, then renders a readiness score, local breakdown chart, check rows, issue/opportunity row-detail dialogs, issues with clickable local public-post links when available, opportunities, and downloadable CSV export including issue public-post paths. Real crawler data, schema validation against live pages, SERP analysis, and exact Blawgy chart/detail fidelity remain comparison/integration work.
- Account load, settings save, POST-backed dashboard actions, and row-level controls now share visible operation/error handling; account controls enter an `aria-busy` state while requests are in flight and preserve controls that were already disabled. The workspace switcher now opens a local selector dialog with available workspace rows, selected status, owner email, role metadata, direct row switching through the protected workspace-select API, and the existing Add site workspace path for mutable workspaces. The workspace actions summary now opens a structured detail dialog covering selected workspace, workspace count, role mix, add-site availability, invite-team state, role-control state, team activity count, and the remaining exact selector/add-flow screenshot gap. Legacy stored UI state is normalized on account load, so unsupported saved views, Settings tabs, or calendar modes fall back to valid dashboard defaults. Legacy stored Settings state is normalized on account load, so unsupported site/image/CMS/CTA dropdown values, malformed URLs, bad booleans, and malformed image-test history do not boot into broken dashboard controls or links. Legacy stored invite/member state is also normalized before account summary and public invite lookup, so invalid collaboration rows are dropped, unsafe member owner roles fall back to member, and stale/external invite links are regenerated as local `/invite/:token` paths.
- The account operation/error live region now sits directly under the hidden metadata topbar instead of inside the Articles panel, so success and failure messages from Billing, Help, Settings, tracking, and other non-Articles panels remain available after button clicks without adding a visible profile bar above the page heading.
- Malformed client invite routes such as `/invite/%` now fall back to the visible missing-invite state instead of throwing before the invite page can render, and the Accept invite button disables for missing, not-found, or already-accepted invite states instead of appearing actionable.
- Dashboard mutation responses that can change derived account state now include a fresh public account snapshot alongside their section-specific payloads, so Getting Started progress, dashboard summaries, and related panels update without a page refresh after settings, content, CMS, inventory feeds, billing, and Search Console actions.
- The sidebar workspace selector now follows the Blawgy sidebar capture more closely with a larger rounded selector card, circular workspace/avatar image treatment with an `S` fallback, truncated workspace label, and stacked up/down chevrons while preserving the existing local workspace switch dialog. The stale legacy 20px workspace-icon override has been removed so the circular selector image treatment survives the stylesheet cascade.
- Settings dirty state now protects unsaved edits with a browser leave warning until the account is saved.
- Site Settings keyword adds now validate an empty draft locally and show an error without sending a settings save; saved keyword chip removal is covered through the dynamic account click harness.
- Getting Started now renders a backend-derived setup checklist with progress, completion state, and jump buttons into the relevant dashboard view/tab instead of a static walkthrough-only list; setup jumps into Settings tabs now push only the intended final account URL instead of an intermediate tab URL. Tours now also includes a covered account-level walkthrough overlay with Start, first-step Back hiding, Back, larger Next-with-chevron, top-right Close, hidden eyebrow, centered wider desktop modal placement, larger rounded modal corners, divider-backed single-row footer with dots/progress/actions, sidebar-step pointer triangle, raised sidebar target highlight with the screenshot-backed right-edge fade on sidebar steps, a full Settings tab-strip highlight for the Site Settings `Customize Your Content` step, a top tab-pointer for settings-tab tour steps, pointerless Content Plan command/filter/bulk steps, and Finish states that remains visible while moving through the screenshot-backed Blawgy sequence: Welcome, Article Command Center, Filter by Status, Bulk Schedule Articles, Write, Topics Worth Writing, Settings, Customize Your Content, CMS Connect, Image Settings, and the finish state, with SBA branding preserved in visible copy. Exact Blawgy highlighted-target geometry and pixel placement remain screenshot comparison work.
- Unsupported item-detail methods and row-detail reads for product, location, topic, and content-plan routes now fall through as missing actions instead of returning false success.
- Topic add is idempotency-safe at the API boundary: adding an already-added topic now returns a conflict instead of duplicating planned articles, and successful adds update the content plan timestamp.
- Singleton and collection parent routes such as Summary, Workspaces, UI, Settings, Write Draft, Members, Support, Billing, Search Console, Rankings, AI Mentions, Invites, and inventory feed now reject unsupported child subpaths instead of letting parent handlers return success.
- Action endpoints now require exact route depth, so extra segments on paths such as workspace select, settings generate-description, image/CMS tests, product/location updates, content-plan generation, billing actions, and Search Console sync fall through instead of performing the action.
- Invite acceptance now also requires the exact `/api/invite/:token/accept` route, so malformed extra-depth invite action URLs do not accept invitations.
- The client and static server routers now treat only decoded exact `/invite/:token` URLs as invite pages; extra-depth URLs such as `/invite/token/extra` and encoded-slash URLs such as `/invite/%2F` render the not-found route instead of an invite shell.
- Dynamic dashboard rows now escape account-provided text before rendering product, topic, content-plan, location, invite, billing, tracking, support, Image Settings, CMS, and inventory feed detail values.
- The local HTML now references `/app.js?v=20260814-pages-generator` and `/styles.css?v=20260819-nav-hover-bridge` so narrow deploys of these account/dashboard changes bust browser caches after the latest nav hover-bridge fix. The account client and server normalization also scrub blocked legacy inventory/source labels from account payloads before Products renders.
- Public Blog, Features, Pricing, Affiliate, Changelog, Privacy, Terms, and Case Study routes now show launch-usable content instead of visible placeholder route copy.
- Public blog publishing now has a conservative first backend slice: `GET /api/blog/posts`, `GET /api/blog/posts/:slug`, `/blog`, and `/blog/:slug` expose sanitized published posts only from the configured public publisher account (`SIR_BLOGGS_PUBLIC_ACCOUNT_ID` takes precedence over the owner-email fallback `SIR_BLOGGS_PUBLIC_OWNER_EMAIL`). Draft, unpublished, future, private owner, and raw internal ids are not exposed. Published account article table rows/cards receive a derived `publicPath` and show `Open`/`Open post` links only for local `/blog/:slug` pages.
- Homepage/internal clicks to `/blog`, `/blog/:slug`, `/reports`, and `/reports/:shareId` now bypass SPA interception so published posts and shared reports are served by the backend-rendered public pages instead of the static client-side route. The global click handler also respects non-self link targets and modified clicks, so generated invite/report dialog links with `target="_blank"` and browser shortcuts such as Cmd-click/Ctrl-click are left to the browser instead of being forced into the current SPA tab.
- Signup is no longer an info-only placeholder: it offers Pro/Pro+ trial CTAs into Google login with one-shot `next=/account?view=billing&checkoutPlan=...&billingPeriod=annual` destinations, signed-in users who visit `/login` or `/signup` are routed to account billing, pricing-card trial CTAs preserve selected Pro/Pro+ plan and Monthly/Annual period, public trial CTAs switch to Account/Billing while signed in, and backend-rendered Blog/Report pages render Account plus a form-backed Log out control for authenticated sessions instead of Login/Sign up. Public signed-in Account links now target `/account?view=plan` so they open the Articles dashboard home instead of restoring an arbitrary saved panel. The local `/success` route now mirrors the Blawgy post-checkout capture with the compact checkout-success header, free-trial banner, `You're all set` headline, exact 10-minute content-plan-building copy, yellow still-building warning box, calendar-icon `Open your content plan` CTA into the protected Content Plan, and website-platform footnote; `/success?state=preparing` preserves the captured preparation state with `Your plan is being prepared`, Scoring/Scheduling/Writing/Putting-together steps, under-a-minute note, and `4s` timer copy. The server-sent app shell now also personalizes signed-in HTML before client hydration, so `/account` and other app-shell routes do not initially show Login/Sign up, hidden Account/Logout controls, anonymous `data-auth-trial` CTA markers, or Start free trial copy to authenticated users; signed-in trial CTAs render as `Open billing` links to `/account?view=billing`. The account Billing portal button now opens a local portal session endpoint and updates billing state; plan buttons and selected signup-plan redirects refresh local checkout/invoice state and surface the latest local invoice in a dialog with the captured Stripe trial framing: `Try Sir Bloggsalot <plan>`, `3 days free`, monthly renewal copy, and the daily-publishing offer line. Real Stripe checkout/payment-method management remains integration work.
- Invalid or stale signup checkout query parameters, such as unsupported `checkoutPlan` values, are now stripped after account load without calling the local checkout endpoint, so malformed signup handoffs do not leave sticky billing params in account URLs. If a valid-looking checkout handoff reaches the billing endpoint but fails, the dashboard shows the protected error once and still removes `checkoutPlan`/`billingPeriod` so account-route renders do not retry the same failed checkout. Read-only shared workspaces also strip checkout handoff params without mutating Billing, preventing member sessions from keeping unusable signup URLs around.
- Billing aliases `/subscribe`, `/upgrade`, and `/billing` preserve the billing-specific `checkoutPlan`, `billingPeriod`, and `invoice` query parameters when canonicalizing to `/account?view=billing`, so direct alias handoffs such as `/subscribe?checkoutPlan=Pro%2B&billingPeriod=annual` still reach the protected Billing checkout flow after login.
- Home-page hash links for Features, Pricing, and FAQ now behave as scroll anchors only and no longer carry stale `data-route` metadata, so active-route state is reserved for real route pages such as Blog, Login, Signup, and Account.
- Logged-out account redirects, account logout redirects, and invite login/accept redirects now preserve the full account or invite path and query string at the server and client boundaries, so `/account?view=billing` returns to Billing after Google login instead of dropping back to the default account view and invite links with query context return to the same invite after sign-in. Signed-in visits to `/login` or `/signup`, plus the Google credential callback itself, honor safe same-origin account paths, dashboard alias `next` paths such as `/keyword-finder` and `/settings/cms-connect`, or exact one-segment invite `next` paths before falling back to Account/Billing, so Help, dashboard aliases, and invite deep links do not get flattened and unsafe/public `next` values are not used after sign-in. Client and server signed-in auth redirects also reject malformed encoded account/invite next paths such as `/invite/%`, encoded-slash invite paths such as `/invite/%2F`, and extra-depth invite paths such as `/invite/token/extra`, then fall back to Account/Billing instead of sending users into a bad route.
- `/api/health` now supports unauthenticated GET and HEAD smoke checks for production deploy verification.
- Real Stripe portal, real CMS publishing/credential validation, real inventory feed API sync, real article generation, real topic discovery, production RBAC for multi-workspace access, and live chat/email support delivery remain integration work.

## Implemented Local Account API

- `GET /api/account/summary`
- `GET /api/account/workspaces`
- `POST /api/account/workspaces`
- `POST /api/account/workspaces/select`
- `PUT /api/account/ui`
- `GET /api/health`
- `HEAD /api/health`
- `GET /api/auth/config`
- `GET /api/auth/session`
- `POST /api/auth/google`
- `POST /api/auth/logout`
- `GET /api/account/settings`
- `PUT /api/account/settings`
- `POST /api/account/settings/generate-description`
- `POST /api/account/images/test`
- `DELETE /api/account/images/test-history`
- `GET /api/account/content-plan`
- `GET /api/account/content-plan/strategy`
- `PUT /api/account/content-plan/strategy`
- `POST /api/account/content-plan/items`
- `GET /api/account/content-plan/items/:id`
- `POST /api/account/content-plan/bulk-schedule`
- `POST /api/account/content-plan/bulk-update`
- `PUT /api/account/content-plan/items/:id`
- `DELETE /api/account/content-plan/items/:id`
- `POST /api/account/content-plan/items/:id/generate`
- `POST /api/account/content-plan/items/:id/publish`
- `GET /api/account/blog/posts`
- `POST /api/account/blog/posts`
- `GET /api/account/blog/posts/:id`
- `PUT /api/account/blog/posts/:id`
- `DELETE /api/account/blog/posts/:id`
- `POST /api/account/blog/posts/:id/generate`
- `POST /api/account/blog/posts/:id/schedule`
- `POST /api/account/blog/posts/:id/publish`
- `POST /api/account/blog/posts/:id/unpublish`
- `GET /api/account/topics`
- `POST /api/account/topics`
- `POST /api/account/topics/search`
- `POST /api/account/topics/export`
- `POST /api/account/topics/save-keywords`
- `PUT /api/account/topics/:id`
- `POST /api/account/topics/:id/add`
- `DELETE /api/account/topics/:id`
- `GET /api/account/write-draft`
- `PUT /api/account/write-draft`
- `POST /api/account/write-draft/preview`
- `GET /api/account/products`
- `POST /api/account/products`
- `PUT /api/account/products/:id`
- `DELETE /api/account/products/:id`
- `GET /api/account/locations`
- `POST /api/account/locations`
- `PUT /api/account/locations/:id`
- `DELETE /api/account/locations/:id`
- `POST /api/account/cms/test`
- `GET /api/account/inventory-feed`
- `POST /api/account/inventory-feed/connect`
- `POST /api/account/inventory-feed/sync`
- `POST /api/account/inventory-feed/disconnect`
- `POST /api/account/cms/connect`
- `POST /api/account/cms/disconnect`
- `GET /api/account/invites`
- `POST /api/account/invites`
- `DELETE /api/account/invites/:id`
- `GET /api/account/members`
- `PUT /api/account/members/:id`
- `DELETE /api/account/members/:id`
- `GET /api/account/support`
- `POST /api/account/support`
- `PUT /api/account/support/:id`
- `GET /api/invite/:token`
- `POST /api/invite/:token/accept`
- `GET /api/account/billing`
- `PUT /api/account/billing`
- `POST /api/account/billing/checkout`
- `GET /api/account/billing/invoices/:id`
- `POST /api/account/billing/portal`
- `POST /api/account/billing/payment-failed`
- `POST /api/account/billing/cancel`
- `POST /api/account/billing/reactivate`
- `GET /api/account/search-console`
- `GET /api/account/search-console/export`
- `POST /api/account/search-console/connect`
- `POST /api/account/search-console/sync`
- `POST /api/account/search-console/disconnect`
- `GET /api/account/rankings`
- `GET /api/account/rankings/export`
- `GET /api/account/ai-mentions`
- `GET /api/account/ai-mentions/export`
- `GET /api/account/reports`
- `GET /api/account/reports/export`
- `POST /api/account/reports/share`
- `POST /api/account/reports/schedule`
- `GET /api/account/seo-analysis`
- `GET /api/account/seo-analysis/export`
- `GET /api/blog/posts`
- `GET /api/blog/posts/:slug`
- `GET /blog`
- `GET /blog/:slug`
- `GET /reports/:shareId`

## Blawgy Screenshots Already Captured

The existing screenshot set already covers these areas well enough for first-pass visual matching:

- Public landing page framing.
- Sidebar and account workspace selector placement.
- Settings top navigation.
- Site Settings first fold, including Product Description, Target Audience, Your keywords, Add, Keyword mix, Generate using AI, and Save Changes.
- Products tab empty state with Inventory Feed integration, Add Product, status filters, search box, and empty state.
- Image Settings tab top-level settings, including include-images toggles, product image usage, Visual Style, and Image Guidelines.
- Expanded in-article image settings with test generation and default guideline text.
- CMS Connect first screen with Website URL and Website Platform select.
- Business Locations empty state and Add a location CTA.
- Call to Action tab with enable toggle, Edit CTA action, and best-practices card.
- Invite Users tab with email input, Generate Invite Link, Current Team Members, and invitation-link instructions.
- Content Plan visual layout behind onboarding overlays.
- Onboarding walkthrough overlay steps: welcome, command center, filter by status, bulk schedule, write, topics, settings, content customization, CMS connection, image customization, and final "all set" step.
- Billing checkout and post-checkout success/preparation screens, including free-trial messaging and content-plan preparation state.

## Screenshots Needed Next From Blawgy

Capture full-screen desktop screenshots unless noted. Redact secrets, payment details, personal addresses, private emails, API keys, and tokens. For forms, it is fine to enter dummy values if the real app allows it without committing changes.

### Content Plan

- Content Plan without any walkthrough overlay.
- Strategy drawer or modal after clicking Strategy.
- Bulk Schedule modal or flow, including its empty/default state and any confirmation state.
- Add topics modal or drawer from the calendar toolbar.
- Article card opened or expanded, including actions, status labels, dates, edit fields, and publish/schedule controls.
- Grid view and list view after switching the toolbar controls.
- Refresh/loading state and any generated-plan error state.
- Status filters if present, especially Scheduled, Processing, Draft, Published, Failed, and Paused.
- Empty content-plan state for a new account if available.

### Write

- Default Write page before any input.
- Article type/template chooser if present.
- Manual brief form fields and exact labels.
- Source inputs, keyword inputs, audience inputs, product/location/CTA selectors, and any advanced options.
- Generate/write button states: disabled, loading, success, and validation error.
- Generated draft/editor screen, including title, outline, body editor, image blocks, SEO metadata, preview, save, publish, schedule, and regenerate controls.
- Any article preview or publish confirmation modal.

### Topics

- Default Topics page.
- Search/discover form and filters.
- Topic results table or cards with all visible columns and metrics.
- Topic detail view exact layout/copy if clicking a topic opens one in Blawgy; SBA now has a local keyword-detail dialog but needs fidelity comparison.
- Add-to-plan behavior before and after adding.
- Empty, loading, no-results, and error states.
- Any controls for excluding topics, regenerating topic ideas, or changing keyword/source mix.

### Settings

- Site Settings below the first fold, including keyword mix labels, slider endpoints, saved keywords chips, delete behavior, competitors, brand voice, writing style, language, schedule/frequency, approval mode, and any validation.
- Save Changes behavior: dirty state, success toast, failure toast, disabled state, and unsaved-change warning if present.
- Generate using AI behavior for Product Description: exact Blawgy modal vs inline behavior, inserted-text layout, and failure state.
- Products Add Product modal and manual product form.
- Product edit/detail screen, product card/list after one product exists, hide/show/delete behavior, and filters.
- Inventory Feed connection flow. Redact credentials and stop before saving anything sensitive.
- Image Settings collapsed and expanded sections, test image generation result, examples, style presets, guideline editor, and failure states.
- CMS platform dropdown opened, showing all available platform options.
- CMS platform-specific forms for WordPress, Webflow, Shopify, Wix, Ghost, and Custom API if present. Redact tokens/passwords.
- CMS connection success, failure, draft-first, and disconnect states.
- Business Location add/edit modal, fields, validation, saved location card, delete confirmation, and service-area fields.
- Call to Action edit modal or page, placement options, CTA preview, enabled/disabled state, validation, and saved CTA card.
- Invite Users generated-link state, generated-link dialog if Blawgy shows one, invalid email error, copy-link behavior, current team member roles/actions, revoke link, remove member, and role selector.

### Track

- Rankings locked Pro+ gate.
- Rankings connected/active state if available: keyword table, trend chart, filters, date ranges, and export controls.
- Search Console disconnected state and Google connection flow.
- Search Console connected dashboard: clicks, impressions, indexed pages, charts, top pages, top queries, date ranges, and disconnect/error states.
- AI Mentions locked Pro+ gate.
- AI Mentions active state if available: source list, mentions table, prompts, model/source filters, 30/90-day range behavior, trend chart, and setup flow.

### Billing And Account

- In-app Billing page, not only Stripe checkout.
- Current trial/subscription card, plan comparison, upgrade/downgrade, cancel flow, invoice history, payment method, billing portal link, and failed payment state.
- Account/workspace selector opened from the sidebar, including exact Blawgy labels, ordering, and whether Blawgy shows owner/role/status metadata.
- Exact Blawgy Create/overflow menu labels, ordering, and destinations, to compare against SBA's local Create menu.
- Any multi-site add/switch workflow beyond SBA's local Add site workspace form, especially limits, billing implications, and default setup state.
- Logged-in top navigation on the public site.
- Sign-up route behavior after login and after logout.
- Help page and chat widget opened.
- Mobile and tablet layouts for sidebar, settings tabs, content plan, write flow, billing, and one long modal/form state such as CMS connect, support ticket create, article detail edit, report schedule, or invoice detail.

## Questions For The Local LLM With Blawgy Access

Answer these with screenshots and short notes:

1. What are the real Blawgy routes for each major view? Example: `/content-plan`, `/write`, `/topics`, `/settings/site-settings`, `/settings/products`.
2. Which sidebar and settings links are normal links versus in-page tab controls?
3. What exact fields are stored for site settings, images, products, locations, CTA, CMS connection, and invitations?
4. Which controls are client-only visual state, and which trigger network requests?
5. What network endpoints and payload shapes appear in DevTools for save, create, update, delete, connect, generate, schedule, and publish actions? Redact all secrets.
6. Which screens are paid-only, Pro+ gated, or unavailable until Search Console/CMS/billing is connected?
7. What default values appear for a brand-new account after checkout but before the content plan finishes building?
8. What error messages appear for invalid email, missing website URL, failed CMS connection, failed topic generation, failed article generation, and failed save?
9. What toast messages or confirmation states appear after successful save, connect, invite, add product, add location, generate article, schedule, publish, and delete?
10. What should happen when a logged-in user clicks Sign up, Log in, Account, Billing, or public nav items?

## Comparison Checklist To Fill In

Use this table while inspecting Blawgy and Sir Bloggsalot. Add screenshot filenames or short notes in the last column.

| Area | Sir Bloggsalot current state | Blawgy evidence needed | Status |
| --- | --- | --- | --- |
| Articles table and calendar | Local persisted grid/list, editable saved strategy text with dismiss support, selectable table rows, covered individual row selection/select-all, Blawgy-style visible filters for scheduled, processing, generated, published, failed/needs-attention, and paused states with advanced zero-count tabs hidden, screenshot-backed first-fold header with visible Strategy, icon-only grid/list/refresh/overflow controls, rolling date-window, and `+ Add topics` controls, default grid/card calendar before account data loads and for new accounts, optional list/table shell through the visible list control, hidden Bulk Schedule and Article Builder compatibility actions until exact overflow/menu placement is captured, selected-row bulk status/clear controls that appear after selection and stay disabled/hidden otherwise, visible `Why this plan` strategy card with dismiss support, bulk schedule actions with date validation, title/keyword/date-time/actions columns, covered compact accessible row actions plus covered row More menu, covered structured article detail modal with readiness guidance and lifecycle actions, covered lifecycle confirmation panels with explicit request/progress/failure/success state copy, covered rolling three-week window controls, add topics, row-level schedule modal, covered calendar-card edit/generate/publish/save/delete actions, covered table edit/save, generate/regenerate, publish/unpublish confirmation, delete, and builder handoff actions | Exact article drawer layout, exact publish/schedule confirmation copy, and exact Blawgy overflow/tools menu labels/order | `partially matches` |
| Blog editor API | Protected `/api/account/blog/posts` facade supports list/detail/create/update/generate/schedule/publish/unpublish/soft-delete over content-plan records, preserves internal links, schema type, canonical URL, author, volume, difficulty, and estimated-visit metadata, the direct content-plan item editor path is covered for detail/generate/publish/update/delete, and table rows have client coverage for schedule, publish/unpublish, generate, delete, loading existing articles into Article Builder, and row-level metadata edits | Blawgy opened-draft UI fidelity, regenerate behavior details, and exact publish/schedule confirmation behavior | `partially matches` |
| Article Builder | Visible panel now uses the Blawgy screenshot `Article Builder` heading and `Generate article` primary action while preserving the internal `write` route and Article Builder compatibility aliases; saves richer article metadata including canonical URL, author, search-volume metrics, difficulty, and estimated visits; validates template/status/source-status/preview metadata/word count/canonical URL/featured image URL; lets canonical and featured-image URLs be explicitly cleared; shows a live article preview with local readiness score/missing fields; covers provider-pending local brief preview, Builder-specific save/preview/generate progress feedback, Article Builder navigation, Preview Brief, Save draft for new and opened articles, fresh Create > New article reset, generate title/keyword validation, protected blog editor post create/update behavior, resuming an existing article from the Articles table without dropping metadata or forcing scheduled/published articles back to draft, a generated-state Save/Schedule/Publish/Regenerate action rail wired to protected article endpoints when a source article exists, a generated editor shell with readiness, schedule, public URL, SEO, and featured-image panels around the draft body, and an Article generation detail dialog summarizing body, SEO, image, schedule/public URL, readiness, preview/provider state, and remaining Blawgy evidence needs | Provider-generated long-form body fidelity, regenerate variant behavior, template-specific fields, and exact Blawgy validation/error states | `partially matches` |
| Keyword Finder | Visible panel now uses the Blawgy screenshot `Keyword Finder` heading, high-value-keyword subtitle, and `keywords found` summary while preserving `/keyword-finder` aliases and keyword-save internals; local metric filters, Blawgy-style visible header actions, Blawgy-style count placement above and inside the filter card, Filters header, difficulty/competition metric bars, visible neutral sort indicators, hidden compatibility action column, backend-backed local Search from the filter search field's Enter key that preserves saved topics with in-panel progress feedback, visible counts, saved-keyword guidance row, covered sortable columns, covered individual row selection/select-all with disabled empty Select All, Magic Select over visible saved or search-result rows with disabled no-visible state, Save Keywords with selected transient search-result keyword text sent with selected ids and disabled zero-selection state, local keyword-detail dialog with metrics and Add-to-plan action, transient search-row edit in local results, Add on transient search rows via persist-then-schedule, transient search-row delete without persisted-topic API calls, downloadable CSV export, full local difficulty options plus CPC/difficulty-score/competition entry in topic create/edit, topic create/edit/add/delete/add-to-plan action coverage, and distinct empty vs filtered states | Real keyword-provider data, pagination, batch management, provider errors, and exact Blawgy discovery/detail fidelity | `partially matches` |
| Site Settings | Saves description, audience, brand voice, competitors, language, publishing cadence, timezone, default publish time, keywords, keyword mix; first-fold ordering and helper copy now match the Blawgy Site Settings screenshot while preserving advanced local fields below; Product Description has the screenshot-backed label/action header row, and `Generate using AI` uses the shared SVG sparkle line icon while preserving the generate-description action; Settings form controls now use regular-weight value text while labels stay bold; `Your keywords` includes the screenshot-backed 0% mix, audience-search-terms, good-keyword guide guidance, and side-by-side keyword textarea/Add row with the taller dark Add button; validates dashboard select/time values and empty keyword drafts; covered Add Keywords, saved keyword removal, and Generate Description actions with local progress feedback; generated descriptions and previews use these fields | Exact Blawgy field defaults, validation copy, Generate using AI modal/failure fidelity, and any schedule controls beyond local timezone/default-time support | `partially matches` |
| Products | Covered manual CRUD, create-time visibility choice, local product-detail dialog with Edit product action, required category validation, price/SKU/audience/featured/source/visibility metadata, hide/show, filter/search with active filter state and distinct empty vs no-match states; populated product rows now include source, featured, and visible/hidden badges; disconnected Products view now matches the Blawgy empty-page structure with a neutral source card using a mint storefront line icon and only `+ Connect` on the source card, plus visible `+ Add Product`, All/Synced/Manual/Hidden filters, search field, and `No products yet` / `Add your first product` empty state; dynamic no-product and no-match empty states now use the Blawgy-style gray cube line icon from the shared SVG sprite instead of a text glyph; hidden product filters/search reset to All/blank in that empty state; covered Inventory feed local metadata connect/sync/disconnect with a connection setup checklist, feed detail dialog, credential-state and secret-handling copy, provider-backed real API limits, in-panel progress feedback, connected-only mock synced products, connected/disconnected/failure badges, and hidden Sync/Disconnect controls while disconnected | Exact Blawgy product detail fields/layout, inventory feed real connection screens, real synced-product states | `partially matches` |
| Image Settings | Generic Image Settings now follows the captured first fold with a collapsed Test Image Generation card, In-Article Images heading/subtitle, In-Article Image Style card, reuse-style checkbox row, In-Article Image Guidelines card, default-guidelines row, and critical-guidelines copy while hidden compatibility wiring preserves local toggles, style preset/aspect ratio/image cadence, raw prompt preview, prompt history metadata, covered Test/Clear history actions with in-panel progress feedback, and disabled Clear history while empty. The `/settings/image-style` section route exposes the screenshot-backed Include Images in Articles, Product Images, Visual Style, Image guidelines, compact reset/suggestion utilities, `Use custom guidelines` toggle, `Add specific constraints` helper copy, empty-state product-image toggle off/disabled behavior, and existing `images.*` bindings. Saved local preview cards distinguish local/provider-pending badges from `Needs attention` provider-error states with stored failure reasons; the provider state panel opens a structured provider detail dialog; latest output and prompt-history rows open structured detail dialogs with preview, prompt, format, save target, status, and provider-backed generation limits; real generated-image success is not faked. | Real generated image thumbnails, live provider failure states, exact Blawgy preset labels, asset-library selection | `partially matches` |
| CMS Connect | Visible first fold follows the captured sparse CMS setup with only Website URL and Website Platform above the fold while local action/status/detail wiring stays hidden for compatibility until provider-form screenshots are captured. Local metadata stores platform, website URL, username/account identifier, blog/collection target metadata, credential presence, draft-first mode, and timestamps without returning secrets; the Connect CMS modal exposes platform-specific labels/copy plus a `Publishing mode` select for `Draft first` versus `Auto-publish after approval`, and its provider checklist names the selected provider's required credential plus the credential-error surface. Covered Connect/Test/Disconnect actions show in-panel progress, disable Test/Disconnect when no CMS metadata exists, require configured setup before test, render provider-pending test results, and display connected/local-setup/error badges with `Needs attention` plus stored failure reasons for provider/setup failures. CMS detail rows open a provider detail dialog with platform-specific URL/admin/target/collection labels, publishing mode, required credential, missing checks, provider status/error surface, secret handling, and remaining target screenshot evidence needs. | Exact Blawgy platform-specific fields, real provider success/failure states, and exact collection/blog target picker behavior | `partially matches` |
| Locations | Business Locations now follows the captured empty-state first fold with the `Business locations` title, Pages-generator-and-Write explanatory copy, centered bordered empty card, screenshot-backed mint shield/check icon, `No locations yet` text, generated-pages-and-articles helper copy, and `+ Add a location` CTA while preserving covered local add/detail/edit/delete, location-detail dialog with Edit location action, required city validation, 2-letter state validation, and saved city/state/address/phone/service area/primary metadata. The Blawgy-referenced Pages destination is route-compatible through `/pages`, `/page-generator`, and `/pages-generator`, rendering local page targets from saved locations and site keywords with per-target `Location` detail and `Draft page` actions that seed Article Builder with a local landing-page brief. | Exact Blawgy field labels, validation copy, saved card/detail layout, and any additional service-area-specific fields | `partially matches` |
| CTA | Call to Action now follows the captured sparse first fold with the left enable toggle, `Call to Action` heading, right-aligned `Edit CTA` action, and CTA Best Practices card using the visible four-line guidance while retaining hidden compatibility markup for local field/preview/reset wiring. Settings save persists enabled/label/text/url/validated placement/validated style/open behavior/tracking label; local preview-detail dialog, Edit CTA modal, covered reset action, and disabled reset while empty/default remain wired through the local settings model. | Real Blawgy edit modal/page fidelity, validation copy, exact style/placement options, article-rendered preview fidelity, and analytics behavior | `partially matches` |
| Invites and members | Invite Users now follows the captured first fold with a single Email Address row, disabled-until-email `Generate Invite Link` button, hidden compatibility role selector, exact manual-send helper copy, Current Team Members heading, expanded invitation-link instructions, Blawgy-style dark circular info icon, horizontal divider, taller guidance card, wider Generate Invite Link action, and full-width Settings panel layout. Accepted member and pending invite rows use grey circular initial chips, pending invite rows use the compact row grid, empty accepted-member/pending-invite/Team Activity blocks stay hidden until rows exist, and local invite/member behavior remains covered: generate/detail/copy/revoke invite, accepted-member detail, role update/remove, Team Activity detail, signed-out invite redirect, missing-token guard, signed-in acceptance before account data loads, enforced local member/editor/admin permissions, workspace selector rows, local owner-scoped Add site workspace flow, and a Workspace actions detail dialog for selected workspace, workspace count, role mix, add-site, invite, role-control, and team-activity state. | Real email delivery, exportable/durable full audit log, exact Blawgy multi-site add flow, selector labels/order, and team-management layout | `partially matches` |
| Billing | Local plan/period/status validation, checkout with refreshed dashboard invoice state, covered billing-period toggle, local portal dialog/status with in-panel progress feedback, failed-payment alert, structured Billing action-detail dialog, structured invoice detail row dialog with payment-handoff copy, invoice deep links, cancel/reactivate progress feedback, and invoices. Captured trial framing is reflected locally: checkout dialogs show `Try Sir Bloggsalot <plan>`, `3 days free`, monthly renewal copy, daily-publishing offer copy, and a saved 3-day `trialEndsAt`; selected signup-plan redirects and plan buttons refresh local invoice state and clean the checkout URL after handoff. The `/success` route mirrors the captured checkout-success flow with compact header, `You're all set`, 10-minute content-plan-building copy, yellow still-building warning, calendar-icon `Open your content plan` CTA, website-platform footnote, and a `/success?state=preparing` preparation variant. | Real Stripe portal, Stripe webhook/payment-method management, upgrade/downgrade fidelity, exact Blawgy billing portal/cancel/invoice details, and real payment confirmation | `partially matches` |
| Google Search Console | Visible panel now uses the Blawgy screenshot `Google Search Console` heading and disconnected status copy while preserving Google OAuth/Search Console provider wiring and `/google-search-console` compatibility; connected header narrows to the date-range selector plus icon-only Disconnect, with Sync/Export kept as hidden compatibility handlers; local validation plus covered connect/sync/disconnect snapshot, protocol-free connected-property subtitle under the page title, disabled dependent controls while disconnected, retained metrics with the extra visible metric-card row and legacy single-list section hidden, 7/28/90-day trend chart with Clicks/Impressions legend chips, two Blawgy-style Top Performing Posts and Top Search Queries cards with Page/Query, Clicks, and Impressions headers plus table-style click/impression rows, local row-detail dialog with query/page label and metrics, clickable local public blog page rows, card-level row-count controls, row search, covered Connect/Sync progress feedback, explicit Google-OAuth-pending copy, and downloadable CSV export with trend rows | Google OAuth flow, provider errors, freshness, exact Blawgy range behavior, and export fidelity | `partially matches` |
| Rankings | Pro+ gate with disabled dependent controls, local active snapshot, summary metrics, improved/declined filters, keyword search, 30/90-day date ranges, average-position chart, row-detail dialog with keyword/URL/position/change/date range, covered refresh/progress/range-error actions, clickable local public blog URL rows, and downloadable CSV export with trend rows | Provider rank tracking data, exact Blawgy chart/range behavior, row-detail fidelity, and export fidelity | `partially matches` |
| AI Mentions | Pro+ gate with disabled dependent controls, local active snapshot, summary metrics, mentioned/monitoring filters, source/model filters, 30/90-day date ranges, prompt/source/model search, mention-count chart, row-detail dialog with source/model/prompt/status/date range, covered refresh progress action, and downloadable CSV export with trend rows | Provider AI/source monitoring data, exact Blawgy setup flow, row-detail fidelity, and export fidelity | `partially matches` |
| Reports | Local content/tracking/ranking/AI mention rollup with overview chart, row-detail dialog with section/label/value/detail/public path, clickable local public-post links for content rows in dashboard and shared report pages, selectable local Performance summary / Content inventory / Executive digest templates, local `/reports/:shareId` shared page, clickable/copyable share-link dialog with in-panel progress feedback, configurable weekly/monthly schedule modal with validated recipients/template selection, delivery setup checklist, structured Reports actions detail dialog, local-only metadata status, provider-backed email-delivery limits, and in-panel save progress feedback, and downloadable CSV export including template metadata plus local public-post paths | Scheduled email delivery, exact Blawgy chart/template/action-modal fidelity, row-detail fidelity, and richer export fidelity | `partially matches` |
| SEO Analysis | Local readiness score, metadata/body/schema/internal-link/published URL checks, setup opportunities, breakdown chart, check rows, issue/opportunity row-detail dialogs, clickable local public-post links for issue rows, and downloadable CSV export including issue public-post paths | Real crawler data, live-page schema validation, SERP analysis, exact Blawgy chart/detail fidelity, and export fidelity | `partially matches` |
| Tours / onboarding | Backend-derived checklist plus covered account-level walkthrough overlay with Start, Back, larger Next-with-chevron, Finish, Blawgy-style 11-step `1 of 11` progress copy, dot indicators, fixed wider modal/backdrop presentation, larger rounded modal corners, divider-backed single-row footer with dots/progress/actions, sidebar-step pointer triangle, raised sidebar target highlight with right-edge fade on screenshot-backed sidebar steps, full Settings tab-strip highlight for the Site Settings step, active-tab highlight plus top tab-pointer on settings-tab tour steps, pointerless Content Plan command/filter/bulk steps, and screenshot-backed step sequence for Content Plan, status filters, Bulk Schedule, Write, Topics, Settings, Site Settings, CMS Connect, Image Settings, and finish while preserving SBA branding | Exact highlighted-target geometry and pixel placement from overlay-free captures | `partially matches` |
| Help | Covered local support options dialog from the floating help bubble with open/total ticket summary, local support ticket create/list, ticket detail/thread dialog, validated categories/priorities, page context, local reply notes, explicit reply form, and resolve/reopen status controls | Live chat provider behavior, ticket/email delivery states, exact Blawgy support modal/chat copy | `intentionally local placeholder` |
| Public routes | Launch content and auth-aware login/signup routing | Blawgy public auth/nav if different | `matches enough for local MVP` |

## Sir Bloggsalot Build Target

Build the account area as a real, persisted, owner-scoped dashboard. Keep the implementation conservative and local to this project.

## Expected Handoff Output

Return one folder or zip containing:

- `README.md` with capture date, Blawgy account type, browser viewport size, and any account setup notes.
- `screenshots/` with descriptive filenames, for example `content-plan-article-drawer.png`, `settings-cms-wordpress-error.png`, and `billing-cancel-confirmation.png`.
- `routes.md` listing Blawgy routes and whether each screen is a route, tab, modal, drawer, or external provider.
- `fields.md` listing each form field, default value, required/optional status, validation errors, and saved-state rendering.
- `network.md` listing safe DevTools endpoint paths, methods, request keys, response keys, status codes, and redacted examples. Do not include tokens, cookies, passwords, API keys, customer data, or payment data.
- `comparison.md` using the table above with `matches`, `partially matches`, `missing`, or `intentionally local placeholder` for every area.
- `build-next.md` with the next concrete Sir Bloggsalot implementation steps in priority order. Each step should cite the screenshot or note that proves the gap, name the target Sir Bloggsalot file or endpoint if obvious, and separate local-MVP work from provider integration work.

### Backend

JSON-backed account storage has been added at `data/account-store.json`, keyed by authenticated user id with workspace access support for accepted invite members. Keep this as the local MVP store, with room to replace it with a database later. Use the `Implemented Local Account API` section above as the authoritative endpoint list.

Security expectations:

- Require an authenticated session on every `/api/account/*` endpoint.
- Scope every read/write to the session user.
- Never send stored secrets back to the browser.
- Never commit secrets or generated local stores.
- Keep Google OAuth tokens, CMS passwords, API keys, Stripe identifiers, and Search Console credentials redacted from logs and docs.

### Frontend

- Load account data from the backend on `/account`.
- Persist settings, toggles, products, locations, CTA, CMS config, invites, and content-plan edits.
- Replace inert controls with real behavior or visibly disabled controls with honest copy.
- Support deep links for account views and settings tabs, either with route paths or query parameters.
- Add forms/modals for Product, Location, CTA, Invite Link, Add Topics, Bulk Schedule, Strategy, and Article details.
- Add loading, empty, error, success, and dirty states for every backend-backed panel.
- Keep Pro+ features honest: show locked gates until billing/plan data exists; do not fabricate ranking or AI mention data.
- Logged-in public nav cleanup: header Login/Sign up hide while Account/Logout show, and public trial CTAs point to Account/Billing while signed in.
- Replace signup placeholder: done locally with signup/pricing plan CTAs into Google login, selected-plan handoff to Billing, and signed-in redirect to Account/Billing. Remaining work is real Stripe checkout selection.

## Remaining Implementation Phases

1. Use the Blawgy capture packet to close fidelity gaps in Article Builder/editor, Topics provider behavior, CMS provider forms, Billing/Stripe flows, tracking provider data, Reports chart/template/export fidelity, Help/live-chat delivery, and mobile layouts.
2. Decide which provider-backed features stay as explicit local placeholders for launch and which need real integrations before deploy. Current local placeholders are honest and covered; do not replace them with fake provider success.
3. Re-run local verification: `npm run check`, authenticated `/account` smoke test, anonymous `/account` redirect, local signup selected-plan handoff, and key dashboard view/tab switching.
4. Deploy only after remote backup/hash comparison. The 2026-08-14 deploy intentionally shipped only changed runtime files (`index.html`, `app.js`, and `server.js`); future deploys should keep that same narrow-file discipline instead of syncing the whole folder.
5. After deploy, verify `/api/health`, anonymous `/account`, authenticated `/account`, `/signup`, cache-busted `/app.js` and `/styles.css`, and browser smoke tests for Article Builder, Topics, Settings, Billing, and Help.

## Acceptance Criteria

- `npm run check` passes, including syntax, route/action coverage, auth helpers, and account API smoke tests.
- Anonymous `/account` redirects to `/login?next=/account`.
- Authenticated `/account` loads the user and persisted account data.
- Signed-in `/login` and `/signup` requests redirect to the safe account or invite destination instead of serving the auth shell.
- Changing each settings tab, saving, reloading, logging out, and logging back in preserves the correct data for that user.
- Product, location, CTA, invite, topic, and content-plan create/edit/delete flows work or are explicitly disabled.
- No visible button pretends to work while doing nothing.
- Internal public links resolve to route pages or homepage anchors.
- No account data leaks across users.
- No secrets are printed, committed, or returned to the frontend.
- Public login/account nav accurately reflects signed-in state.
- Mobile layouts do not overlap or hide critical controls.
- Public production checks pass after deploy, with remote files backed up before replacement.
